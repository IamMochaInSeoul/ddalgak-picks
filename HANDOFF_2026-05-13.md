# 딸깍픽스 신규 세션 핸드오프 프롬프트

> 이 파일을 새 Claude 세션의 첫 메시지로 그대로 붙여넣으세요.  
> 작성일: 2026-05-13 | 기준 버전: v0.5.4-stable

---

## 복사 시작 ↓

---

# 딸깍픽스 (ddalgak-picks) 개발 세션 인계

안녕하세요. 이전 세션에서 이어서 작업하는 프로젝트입니다. 아래 컨텍스트를 먼저 숙지한 뒤 작업을 시작해주세요.

---

## 프로젝트 기본 정보

| 항목 | 내용 |
|------|------|
| **제품명** | 딸깍픽스 (ddalgak-picks) |
| **설명** | 수백~수천 장 사진에서 AI가 베스트컷을 골라 폴더 구조 보존 ZIP으로 돌려주는 100% 브라우저 앱 |
| **배포 URL** | https://ddalgak-picks.vercel.app |
| **GitHub** | https://github.com/IamMochaInSeoul/ddalgak-picks |
| **로컬 경로** | `/Users/mocha.bun/Documents/Claude/Projects/vibe coding/ddalgak-picks` |
| **현재 버전** | v0.5.4-stable (main 브랜치에 머지됨) |
| **스택** | Vite 5 + React 18 + TypeScript + Zustand 5 + MediaPipe + JSZip + IndexedDB/OPFS |
| **배포** | Vercel 정적 배포 (소스 push → Vercel 서버 빌드, 로컬 빌드 절대 금지) |

---

## 핵심 제품 개념

### 두 가지 플로우

**Flow A — "사진만 셀렉" (Landing 왼쪽 카드)**
```
Landing → TypeSelect(인물/반려동물/혼합) → Upload → Analysis → [PersonSelect] → Gallery → ZIP
```

**Flow B/C — "스튜디오용 셀렉" (Landing 오른쪽 카드)**
```
Landing → StudioTypeSelect → FolderUpload(다중 폴더 드롭) → 폴더별 순차 분석 → [PersonSelect] → FolderGallery → ZIP
```
**폴더 구조 보존 ZIP이 메인 타겟의 핵심 니즈.**

### 분석 파이프라인 요약
```
파일 입력 → 썸네일 생성 → pHash(연사 그룹핑/씬 클러스터링) → 얼굴 감지(FaceLandmarker×4 + BlazeFace)
→ B급 정밀화(EAR·Laplacian·저조도 노이즈) → 얼굴 임베딩(face-api.js) → 인물 클러스터링
→ 응시 추정 → heroBonus 채점 → 그리디 선별 → PhotoEntry[] 출력
```

### 채점 가중치 (인물 모드)
- eyeOpen 35% / sharpness 30% / expression 20% / facing 15%
- heroBonus clamp(−10 ~ +25): AND 모두 등장 +25, OR 한 명 +15, 미등장 −10

---

## 절대 규칙 (반드시 지킬 것)

```
❌ dist/ 커밋 금지 (API 키 노출 — 2026-04-29 실제 사고 발생)
❌ npx vercel --prod 로컬 직접 빌드 배포 금지
❌ main 브랜치 직접 커밋 금지 (브랜치 → PR → 머지 필수)
❌ payment.ts 수정 금지
❌ analyzer.ts 수정 금지
❌ PhotoDetailModal.tsx 수정 금지
❌ supabase/functions/verify-payment/ 수정 금지
❌ 한국어 카피 코드 내 하드코딩 금지 (messages/ko.json만 사용)
❌ VITE_ 붙은 서버 시크릿 코드에 하드코딩 금지
```

### PRE_PUSH 보안 체크 (커밋 전 항상 실행)
```bash
git diff HEAD src/lib/payment.ts | wc -l          # 0이어야 함
git diff HEAD src/lib/analyzer.ts | wc -l          # 0이어야 함
git diff HEAD src/components/PhotoDetailModal.tsx | wc -l  # 0이어야 함
grep -r "VITE_PORTONE_API_SECRET\|VITE_SUPABASE_SERVICE" src/ | wc -l  # 0이어야 함
git status --short | grep "^A.*\.env$" | wc -l    # 0이어야 함
npx tsc --noEmit                                   # EXIT:0 필수
npx vite build --outDir /tmp/ddalgak-buildN        # EXIT:0 필수
```

### 배포 절차
```bash
git checkout -b feat/vX.X.X-설명
# 작업...
git add src/ public/ index.html vercel.json package.json  # dist/ 절대 제외
git commit -m "feat: ..."
git push -u origin feat/vX.X.X-설명
# GitHub에서 PR → 머지 완료 후:
git tag vX.X.X-stable && git push origin vX.X.X-stable
```

---

## 코드 규칙

### Zustand 셀렉터 — React Error #185 방지
```typescript
// ❌ 절대 금지
const { a, b } = useStore((s) => ({ a: s.a, b: s.b }))
// ✅ 반드시 개별 셀렉터
const a = useStore((s) => s.a)
const b = useStore((s) => s.b)
```

### 폰트 정책 §9-5 (v0.5.2.1 이후 — 위반 시 반드시 수정)
```typescript
// ❌ 위반
fontFamily: "var(--font-display)", fontStyle: "italic"
// ✅ 준수
fontFamily: "var(--font-sans)", fontWeight: 800, fontStyle: "normal"
```

### 카피 원칙
- 이모지·느낌표 0개 (큐레이터 노트 톤)
- 한국어 하드코딩 금지 → `messages/ko.json`만 사용
- AI 마케팅 금지어 없애기: "AI가 알아서", "완벽한", "최고의" 등

### 환경변수 분기 패턴
```typescript
// ❌ 직접 참조 금지 (UI 카피·베타 판단에서)
import.meta.env.VITE_FEATURE_PAYMENT !== "true"
// ✅ freeBetaConfig.ts 함수 사용
import { isFreeBeta, isPaymentEnabled } from "../lib/freeBetaConfig"
```

---

## 현재 구현된 주요 라이브러리/파일

| 파일 | 역할 |
|------|------|
| `src/lib/freeBetaConfig.ts` | 무료 베타 플래그 + 카피 상수 |
| `src/lib/analytics.ts` | GA4 12 이벤트 + PII 스크러버 (noop 상태) |
| `src/lib/zipManager.ts` | IndexedDB ZIP 24h 캐시 |
| `src/lib/opfsStore.ts` | OPFS 폴더 세션 영속화 |
| `src/lib/sessionPersist.ts` | IndexedDB 갤러리 자동저장 |
| `src/lib/userProfile.ts` | 닉네임·sessionCount·heroPersonNames |
| `src/lib/payment.ts` | PortOne V2 (⚠️ 수정 금지) |
| `src/lib/analyzer.ts` | 분석 파이프라인 (⚠️ 수정 금지) |
| `src/components/PhotoDetailModal.tsx` | NOTES 톤 상세 모달 (⚠️ 수정 금지) |
| `src/components/OnboardingModal.tsx` | 첫 방문 안내 |
| `src/components/ReturningBanner.tsx` | 재방문 4단계 호명 |
| `PRODUCT_DOCUMENT.md` | 전체 제품 문서 (기획~구현~로드맵) |

---

## 환경변수 현황

| 변수 | 상태 | 비고 |
|------|------|------|
| `VITE_GOOGLE_CLIENT_ID` | ✅ 등록 | Drive OAuth |
| `VITE_GOOGLE_API_KEY` | ✅ 등록 | 도메인 제한 적용 |
| `VITE_FEATURE_PAYMENT` | `false` | 무료 베타 중 |
| `VITE_GA_MEASUREMENT_ID` | ❌ 미발급 | 발급 후 Vercel에 등록 필요 |
| `VITE_PORTONE_STORE_ID` | ❌ 미등록 | 결제 비활성 중 |
| `VITE_ADSENSE_CLIENT_ID` | ✅ 등록 | 재심사 대기 중 |
| `VITE_SUPABASE_URL` / `ANON_KEY` | ⚠️ 미등록 | 소셜 로그인 전까지 불필요 |

---

## 버전 이력 요약

| 태그 | 주요 내용 |
|------|----------|
| v0.1.1 | MVP (Flow A, 취향 재추출, 세션 지속성, 앨범) |
| v0.3.1-stable | Flow B/C, Google Drive, 보안 사고·체계 확립 |
| v0.4.0-stable | 큐레이터 톤 디자인 토큰, 분석 스토리텔링 |
| v0.4.1-stable | B급 정밀화, GroupCompareModal, NicknameCaptureModal |
| v0.5.0-stage-c | F18 인물 클러스터링·주인공 선택·heroBonus |
| v0.5.2-stable | Drive 병렬화, OPFS 세션 영속화, F18 default-on |
| v0.5.3-stable | PhotoDetailModal (NOTES 톤, 그룹 베스트 점프) |
| **v0.5.4-stable** | **무료 베타 카피, OnboardingModal, ReturningBanner, ZIP 24h 캐시, GA4 12 이벤트** |

---

## 다음 작업 후보 (우선순위 순)

### 1순위 — GA4 측정 ID 발급 (즉시 처리 가능)
`analytics.ts`는 완성됨. Google Analytics에서 G-XXXXXXXXXX 발급 후 Vercel 환경변수 `VITE_GA_MEASUREMENT_ID`에 등록하면 끝.

### 2순위 — v0.6.0: 소셜 로그인
`CLAUDE_CODE_PROMPT_v0.6.0_SOCIAL_LOGIN_PREP.md` 사전 가이드 참조.
- Supabase Auth (Google / 카카오 / 네이버 OAuth)
- 게스트 모드 호환 유지
- 기존 닉네임·heroPersonNames 영속화 보존
- 결제 회원 구분 준비

### 3순위 — 결제 활성화
사업자등록 완료 후: `VITE_FEATURE_PAYMENT=true` + PortOne 환경변수 등록만으로 즉시 활성화 가능 (코드 이미 완성).

### 4순위 — Phase 1 후순위
- 취향 학습 영속화 (EventTag별 preferenceProfile IndexedDB)
- AI 보정 (Replicate API + CodeFormer)

---

## 프로젝트 전체 문서 위치
- **`PRODUCT_DOCUMENT.md`** — 기획부터 현재까지 전체 내용 (908줄)
- **`PHASE1_PLAN.md`** — Phase 1 상세 기획 (KPI·가격·우선순위)
- **`PRE_PUSH_SECURITY_CHECKLIST.md`** — push 전 8단계 보안 게이트
- **`SECURITY.md`** — 보안 정책 + 2026-04-29 사고 기록
- **`DESIGN_DIRECTION.md`** — 큐레이터 톤 디자인 명세 v2

---

## 확인 명령어

```bash
# 현재 브랜치·상태 확인
cd "/Users/mocha.bun/Documents/Claude/Projects/vibe coding/ddalgak-picks"
git status && git log --oneline -5

# 타입 체크
npx tsc --noEmit

# 빌드 검증
npx vite build --outDir /tmp/ddalgak-build-check
```

---

지금 어떤 작업을 진행할지 알려주시면 바로 시작하겠습니다.

---

## 복사 끝 ↑
