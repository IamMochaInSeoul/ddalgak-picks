# 딸깍픽스 디자인 방향 v2.2: "Quiet Precision"

> **작성일:** 2026-05-14
> **상위 문서:** `PRODUCT_DOCUMENT.md`, `PHASE1_PLAN.md`
> **이 문서의 목적:** v1·v2의 "다크룸의 큐레이터" 컨셉을 폐기하고, 라이트 메인 + 다크 보조의 새 시각 시스템으로 전면 재설계한다. 코드 마이그레이션은 별도 PR에서 진행하며, 이 문서가 그 기준이 된다.
> **이전 버전:** v2.1 (2026-05-08, "다크룸의 큐레이터" + 잉크 블루 액센트 + 산세리프 헤드라인) — **본 문서로 대체됨**

---

## 0. 한 문장 컨셉

> **Quiet Precision — 사진이 주인공이 되도록 시스템은 한 발 물러난다.**
>
> 사용자는 "도구를 다루는 중"이 아니라 "**자기 사진을 보는 중**"이라고 느껴야 한다. 시스템 UI는 공기처럼 가볍게, 정밀함은 디테일 안에 숨어 있어야 한다.

이 한 줄이 모든 시각·카피 결정의 기준이다. 갈림길마다 묻는다: *"이게 Apple/Arc/Stripe 같은가, 일반 SaaS 같은가?"*

---

## 1. v2.1까지의 결정 폐기 선언

v0.4.0~v0.5.4의 "다크룸의 큐레이터" 시스템은 두 가지 통증을 만들었다:

1. **컨셉 톤과 실제 결과의 불일치.** "큐레이터·도록·세리프"라는 추상적 비유가 화면에서는 차가운 네이비 + 갇힌 다크 + 글자 흐림으로 구현됐다. 사용자가 진단한 표현: *"네이비톤 배경에 검은 텍스트, 정리되지 않은 느낌."*
2. **사진 셀렉 도구라는 본질과 충돌.** 사용자가 1,000장의 사진을 30분간 보는 화면이라면, **시스템 UI 자체의 컬러·톤이 사진을 침범해서는 안 된다.** 다크 베이스는 사진과 너무 강하게 경쟁한다.

### 폐기되는 항목

| 항목 | v2.1까지 | v2.2 |
|------|---------|------|
| 메인 무드 | "다크룸의 큐레이터" | "Quiet Precision" |
| 배경 정책 | 다크 디폴트·라이트 모드 미개발 | **라이트 메인 + 다크 보조** |
| 헤드라인 폰트 | 산세리프 800 (v2.1에서 세리프 폐기) | **유지** (Pretendard 700~800) |
| 액센트 | 잉크 블루 `#2D4356` | **재선정** (§4-2 후보 3안 → 사용자 확정) |
| 무드 레퍼런스 | Lightroom · Magnum · MoMA · 박물관 도록 | **Apple · Arc · Stripe** |
| 카피 컨셉 | 큐레이터 톤 (도록 캡션·NOTES·"완료.") | **차분한 어시스턴트** — 명확함·신뢰감·자랑하지 않음 |
| 사진 모서리 | 직각 (`--radius-photo: 0`) | **2~4px 미세 라운드** (Apple/Arc 톤) |
| 모션 | 절제·페이드 only | **부드러운 ease + 매우 옅은 그림자 호버** |

### 유지되는 항목

- 이모지 추방 (§3-6에서 § 5-3으로 이동)
- AI 마케팅 금지어 사전 (§5-3에 통합)
- "AI" 단어 페이지당 0~1회 사용 한도
- 모바일 적응 정책 (브레이크포인트 그대로)
- 모노 폰트(JetBrains Mono)는 숫자·메타데이터에 한정

### 폐기되지 않는 가드레일 (절대 규칙)

```
✗ payment.ts 수정 금지
✗ analyzer.ts 수정 금지
✗ PhotoDetailModal.tsx는 v2.2 마이그레이션 PR에서만 수정 (NOTES → 새 사양으로)
✗ dist/ 커밋 금지
✗ 한국어 카피 코드 내 하드코딩 금지 → messages/ko.json
✗ VITE_ 서버 시크릿 하드코딩 금지
```

이 가드레일은 `AGENTS.md`·`SECURITY.md`·`CLAUDE.md`에 그대로 살아 있다. 이 문서는 시각·카피 사양만 갈아엎는다.

---

## 2. 무드 레퍼런스

### 2-1. 닮을 곳 — Apple · Arc · Stripe 교집합

| 레퍼런스 | 차용할 요소 |
|----------|------------|
| **Apple (macOS Sonoma+, iOS 17+)** | 시스템 친화 산세리프(SF Pro / Pretendard), 매우 옅은 그림자, 큰 여백, 차분한 그라데이션, 컨텐츠 우선 |
| **Apple (App Store · iCloud · Music)** | 카드 모서리 8~12px, 라이트 베이스, 보조 정보의 회색 단계 |
| **Arc (browser, Arc Search)** | 가벼운 컬러 액센트(파랑·오렌지·자주)의 절제된 사용, 곡선·둥근 모서리와 직각의 혼용, 매트 한 따뜻한 회색 베이스 |
| **Stripe (대시보드·docs·marketing)** | 명확한 위계, 모노 숫자, 보조 색의 위계화(primary/secondary/tertiary), 스크롤에 따른 미세 모션 |
| **Stripe (Atlas·Press 페이지)** | 절제된 보라/블루 그라데이션, 큰 헤드라인 + 작은 본문, 표 형식의 데이터 표기 |
| **Linear (보조 참고)** | 키보드 우선·정렬·정확성. 메인 톤은 아니지만 디테일에 차용 |
| **Things 3 (보조 참고)** | 침착한 인터페이스·줄어든 그림자 |

### 2-2. 피할 것

- **이전 v2.1의 차가운 네이비 베이스** — `#0D0F12` 계열 폐기
- **세리프 디스플레이 (Spectral·Noto Serif KR)** — 톤 통일 깨짐 (v2.1에서 이미 폐기됨, 본 문서에서 재확인)
- **그라데이션 보라 CTA + 그림자 통통** — SaaS 친화 톤
- **이모지 헤더** — v2 그대로 유지
- **둥근 카드 + 큰 그림자 + 호버 통통** — Airbnb·Notion 마켓플레이스 톤
- **무지개 그라데이션·"Magic" 단어** — AI 마법사 톤

### 2-3. "세련된"의 정의 — 측정 가능한 기준

추상적인 "세련됨" 대신 다음 5가지로 정의한다:

1. **한 화면에 5개 미만의 색.** 액센트는 단일, 회색은 4단계, 상태색은 2개(critical·success).
2. **호버 시 위치가 움직이지 않는다.** `transform: translateY` 금지. 색·보더·그림자 강도만 변한다.
3. **헤드라인 ≠ 본문 ≠ 메타 정보의 시각 위계가 한눈에 들어온다.** 폰트 사이즈 차이 ≥ 1.4배.
4. **모든 텍스트가 WCAG AA(4.5:1) 이상의 콘트라스트.** §6에 명시.
5. **로딩·전환에 부드러운 ease (200~400ms).** 짧고 빠른 모션은 가벼움, 너무 느리면 답답함.

---

## 3. 비주얼 토큰

### 3-1. 컬러 — 라이트 베이스, 단일 액센트, 다크 보조

```css
:root {
  /* ── Surface (라이트 메인) ────────────────────────────────────────── */
  /* 따뜻한 오프화이트. 순백 #FFF는 너무 차갑고, 사진과 콘트라스트가 약하다. */
  --bg-base:        #FAFAF8;   /* 메인 배경 */
  --bg-elevated:    #FFFFFF;   /* 카드·패널 — 순백으로 한 단계 띄움 */
  --bg-overlay:     #F2F1ED;   /* 호버 배경·드롭다운·미세 강조 */
  --bg-sunken:      #EFEEEA;   /* 인풋·코드 블록 등 한 단계 내림 */

  /* ── Surface (다크 보조 — 사진 우선 화면) ───────────────────────── */
  /* PhotoDetailModal·풀스크린 뷰어·갤러리 옵션 다크모드에서만 사용 */
  --bg-base-dark:     #0F0F10;
  --bg-elevated-dark: #18181A;
  --bg-overlay-dark:  #222225;

  /* ── Borders ──────────────────────────────────────────────────────── */
  --border-subtle: #E5E4E0;   /* 기본 구분선 — 거의 안 보이게 */
  --border-strong: #C8C6BF;   /* 강조 구분선·인풋 */
  --border-focus:  var(--accent);

  /* 다크 보조의 보더 */
  --border-subtle-dark: #2A2A2D;
  --border-strong-dark: #3D3D40;

  /* ── Text (라이트 베이스 기준) ───────────────────────────────────── */
  /* 순흑 #000은 차가워서 피한다. 본문은 거의 검정, 보조는 단계별 회색. */
  --text-primary:   #0A0A0B;   /* 본문·헤드라인 — 16.8:1 vs bg-base */
  --text-secondary: #56565A;   /* 보조 정보·라벨   — 7.2:1 vs bg-base (AA 통과) */
  --text-tertiary:  #8A8A8E;   /* 메타·캡션·타임스탬프 — 4.7:1 vs bg-base (AA 통과) */
  --text-disabled:  #C0C0BD;   /* 비활성 — 의도적으로 콘트라스트 약하게 */
  --text-on-accent: #FFFFFF;   /* 액센트 위 텍스트 */

  /* 다크 보조의 텍스트 */
  --text-primary-dark:   #F4F4F5;
  --text-secondary-dark: #A8A8AC;
  --text-tertiary-dark:  #6D6D72;

  /* ── Accent (단일 — Graphite 확정 2026-05-14) ─────────────────────── */
  /* 모노크롬 액센트. 사진을 위해 시스템은 최대한 양보. */
  /* 진행 상태·성공·오류 신호는 --success, --warning, --critical로 보강. */
  --accent:       #1A1A1B;
  --accent-hover: #000000;
  --accent-soft:  rgba(26, 26, 27, 0.10);    /* 호버 배경·뱃지 */
  --accent-quiet: rgba(26, 26, 27, 0.04);    /* 매우 옅은 배경 강조 */

  /* ── Status ───────────────────────────────────────────────────────── */
  --critical:      #D94B3D;   /* 옅은 옥스블러드 → 라이트 위에서도 차분히 보임 */
  --critical-soft: rgba(217, 75, 61, 0.10);
  --success:       #2F8F5F;   /* 차분한 녹 — 형광 그린 금지 */
  --success-soft:  rgba(47, 143, 95, 0.10);
  --warning:       #B47B1E;   /* 황토 — 노랑은 라이트 배경에서 약하므로 톤 다운 */

  /* ── Photo overlay (사진 위 메타 — 모달·갤러리 hover) ────────────── */
  --photo-overlay-light: rgba(255, 255, 255, 0.88);  /* 라이트 사진 위 */
  --photo-overlay-dark:  rgba(15, 15, 16, 0.80);     /* 다크 사진 위 */

  /* ── Shadows (Apple/Stripe 톤 — 매우 옅게, 위계만) ───────────────── */
  --shadow-xs:     0 1px 2px rgba(10, 10, 11, 0.04);                                /* 인풋·작은 카드 */
  --shadow-sm:     0 1px 3px rgba(10, 10, 11, 0.06), 0 1px 2px rgba(10, 10, 11, 0.04); /* 카드 호버 */
  --shadow-md:     0 4px 12px rgba(10, 10, 11, 0.08), 0 2px 4px rgba(10, 10, 11, 0.04); /* 떠 있는 카드 */
  --shadow-lg:     0 12px 32px rgba(10, 10, 11, 0.12), 0 4px 8px rgba(10, 10, 11, 0.06);/* 드롭다운 */
  --shadow-modal:  0 24px 64px rgba(10, 10, 11, 0.20), 0 8px 16px rgba(10, 10, 11, 0.08);/* 모달 */
}
```

**핵심 원칙:**

1. **메인 UI(랜딩·분석·갤러리 리스트·결제·온보딩)는 모두 라이트.** 다크 모드 토글 없음.
2. **다크는 사진 우선 화면에만.** PhotoDetailModal(사진 한 장 전체 화면), 풀스크린 뷰어, 사진 비교 모달.
3. **액센트 컬러는 단일.** 결제 CTA·진행 중인 상태·중요 강조에만. 일반 버튼은 모노크롬(`bg-elevated` + `border-strong`).
4. **그림자는 깊이 표현용, 강조용 아님.** 호버 시 그림자 강해지는 것 OK, scale·translate는 금지.

### 3-2. 액센트 컬러 — Graphite 확정 (2026-05-14)

후보 3안(Iris Violet · Cobalt · Graphite) 중 **Graphite `#1A1A1B` (모노크롬 액센트)** 로 확정.

| 속성 | 값 |
|---|---|
| 메인 (`--accent`) | `#1A1A1B` (거의 검정, 순흑 아님) |
| Hover (`--accent-hover`) | `#000000` |
| Soft 10% (`--accent-soft`) | `rgba(26, 26, 27, 0.10)` — 호버 배경·뱃지 |
| Quiet 4% (`--accent-quiet`) | `rgba(26, 26, 27, 0.04)` — 매우 옅은 강조 |
| 인상 | Arc · 일본 무인양품 · 완전한 절제 |
| 콘트라스트 vs `--bg-base` (#FAFAF8) | **16.5:1** (AAA) |
| 콘트라스트 `--text-on-accent` (#FFFFFF) vs `--accent` | **18.7:1** (AAA) |

**선택 사유:** 사진이 주인공이 되도록 시스템 컬러를 최대한 양보. "Quiet Precision" 컨셉의 가장 충실한 구현. 컬러 액센트가 사진의 채도·색감과 경쟁하지 않음.

**대가 — 상태 신호 보강 필요:**
모노크롬 액센트는 진행 상태·성공·오류 등 "지금 무엇이 일어나고 있는지"를 색만으로 전달하기 어렵다. 따라서 다음 규칙을 함께 적용:

| 신호 유형 | 표현 방법 |
|----------|----------|
| **결제 CTA** | `--accent` 배경 + `--text-on-accent` 흰 글자 (자체 콘트라스트로 충분) |
| **진행 중 (분석·ZIP)** | `--accent` 진행률 바 + 모노 `%` 텍스트 + subtle 애니메이션 |
| **선택됨 (사진 카드)** | `--accent` 2px 보더 + 좌상단 `--accent` 체크 아이콘 |
| **호버** | `--shadow-sm` 그림자 추가 + `--border-strong` 보더 |
| **성공 (다운로드 완료 등)** | `--success` 텍스트 (#2F8F5F) — 색으로 전달 |
| **오류 (결제 실패 등)** | `--critical` 텍스트 + 보더 (#D94B3D) — 색으로 전달 |
| **경고 (베타·약관 등)** | `--warning` 텍스트 (#B47B1E) |

**핵심:** 모노크롬 액센트라고 해서 화면이 흑백이 되는 것은 아니다. 사진이 컬러를 채우고, 상태 신호는 status 토큰이 채운다. 액센트는 "**이게 메인 행동이다**"라는 위계 표시 역할만 한다.

### 3-3. 타이포그래피

```css
:root {
  /* 시스템 친화 산세리프 — Apple/Arc/Stripe 공통 톤 */
  --font-sans:
    "Pretendard Variable", "Pretendard",
    -apple-system, BlinkMacSystemFont, "Inter",
    "Segoe UI", "Noto Sans KR", sans-serif;

  /* 모노 — 숫자·데이터·코드 */
  --font-mono:
    "JetBrains Mono", "IBM Plex Mono", "SF Mono",
    ui-monospace, monospace;

  /* (참고) v2.2에서 세리프 디스플레이 토큰은 완전히 제거 */
  /* --font-display 토큰 자체를 tokens.css에서 삭제 — 잔존 참조는 grep으로 확인 */

  /* ── 사이즈 스케일 (Apple HIG·Stripe 톤) ──────────────────────────── */
  --text-2xs:    11px;   /* 라벨 대문자 트래킹 */
  --text-xs:     12px;   /* 메타·캡션 */
  --text-sm:     13px;   /* UI 라벨·보조 텍스트 */
  --text-base:   15px;   /* 본문 */
  --text-md:     17px;   /* 부제·강조 본문 */
  --text-lg:     20px;   /* H3 */
  --text-xl:     clamp(24px, 2.4vw, 28px);   /* H2 */
  --text-2xl:    clamp(28px, 3.2vw, 36px);   /* H1 */
  --display-1:   clamp(40px, 5.5vw, 64px);   /* 랜딩 헤드라인 */
  --display-2:   clamp(48px, 7.5vw, 88px);   /* 갤러리 카운터 */

  /* ── 굵기 (Pretendard Variable 활용) ──────────────────────────────── */
  --weight-regular:   400;
  --weight-medium:    500;
  --weight-semibold:  600;
  --weight-bold:      700;   /* 헤드라인 디폴트 */
  --weight-extrabold: 800;   /* 디스플레이·갤러리 카운터 */

  /* ── 행간 ────────────────────────────────────────────────────────── */
  --leading-tight:  1.15;   /* 디스플레이·헤드라인 */
  --leading-snug:   1.35;   /* 부제·라벨 */
  --leading-normal: 1.55;   /* 본문 */
  --leading-relaxed: 1.7;   /* 긴 본문(설명 단락) */

  /* ── 자간 ────────────────────────────────────────────────────────── */
  --tracking-tight:     -0.02em;   /* 디스플레이·헤드라인 */
  --tracking-snug:      -0.01em;   /* 부제 */
  --tracking-normal:     0em;
  --tracking-wide:       0.04em;
  --tracking-uppercase:  0.10em;   /* 라벨용 대문자 */
}
```

**적용 규칙:**

1. **헤드라인은 Pretendard 700~800**, 자간 -0.02em, 행간 1.15. Apple/Stripe 헤드라인 톤.
2. **본문은 Pretendard 400~500, 15~17px**, 행간 1.55. 가독성 우선.
3. **모노는 숫자·파일명·EXIF·진행률에만.** 카운터(`80`, `1,247`), %, 사이즈(`312 MB`), 시간(`4분 17초`).
4. **대문자 트래킹 라벨**(`tracking-uppercase`)은 섹션 헤더·뱃지에만. 본문 사용 금지.
5. **이탤릭 한국어 금지.** v2.1 결정 유지 (Pretendard italic은 한글에서 어색).

### 3-4. 공간 — Apple/Stripe 톤의 시원한 여백

```css
:root {
  --space-0:  0;
  --space-1:  4px;
  --space-2:  8px;
  --space-3:  12px;
  --space-4:  16px;
  --space-5:  20px;
  --space-6:  24px;
  --space-8:  32px;
  --space-10: 40px;
  --space-12: 48px;
  --space-16: clamp(40px, 5vw, 64px);
  --space-20: clamp(48px, 6vw, 80px);
  --space-24: clamp(64px, 8vw, 96px);
  --space-32: clamp(80px, 10vw, 128px);

  /* 컨테이너 max-width */
  --container-narrow: 720px;    /* 본문·읽기 화면 */
  --container-base:   960px;    /* 랜딩·결제 */
  --container-wide:   1280px;   /* 갤러리·작업 화면 */
  --container-full:   100%;     /* 풀스크린 모달 */
}
```

**원칙:**

- **콘텐츠 max-width 적용.** 와이드 화면(>1440)에서 콘텐츠가 끝없이 늘어나지 않게.
- **카드 그림자 대신 여백으로 구분.** 단, 카드는 `--bg-elevated`(흰색)로 한 단계 띄워서 시각적 분리.
- **섹션 간 여백은 시원하게.** 랜딩에서 헤드라인-드롭존-푸터 간 `--space-24` 이상.

### 3-5. 모서리·그림자

```css
:root {
  --radius-xs:    2px;    /* 뱃지·작은 인풋 */
  --radius-sm:    4px;    /* 인풋·작은 카드 */
  --radius-md:    8px;    /* 카드·버튼 — Apple/Arc 톤 */
  --radius-lg:    12px;   /* 큰 카드·드롭다운 */
  --radius-xl:    16px;   /* 모달 */
  --radius-photo: 4px;    /* 사진 미세 라운드 — v2.1의 직각 폐기. Apple/Arc 톤 */
  --radius-full:  9999px; /* pill 버튼·아바타 */
}
```

**중요 변경:**
- **v2.1 `--radius-photo: 0`(직각) → v2.2 `4px`(미세 라운드).** Apple/Arc는 사진을 살짝 둥글게 처리한다. 폴라로이드 톤이 되지 않게 4px만.
- **버튼·카드는 8px.** Arc·Stripe 톤.
- **모달은 16px.** Apple iOS 시트 톤.

그림자는 §3-1의 `--shadow-*` 5단계 그대로. **호버는 그림자 강도만 한 단계 올림** (예: `xs → sm`).

### 3-6. 모션 — 부드러움·자연스러움

```css
:root {
  /* Apple/Stripe의 부드러운 ease */
  --ease-standard: cubic-bezier(0.32, 0.72, 0, 1);    /* Apple-style */
  --ease-decel:    cubic-bezier(0.0, 0.0, 0.2, 1);
  --ease-accel:    cubic-bezier(0.4, 0.0, 1, 1);
  --ease-spring:   cubic-bezier(0.34, 1.56, 0.64, 1); /* 미세한 스프링 — 모달 등장 등 */

  --dur-instant: 80ms;    /* 클릭 피드백 */
  --dur-fast:    160ms;   /* 호버 */
  --dur-base:    240ms;   /* 모달·탭 전환 */
  --dur-slow:    360ms;   /* 페이지 전환 */
  --dur-slower:  560ms;   /* 큰 reveal·갤러리 stagger 마지막 */
}
```

**허용:**
- 페이드 인 + 살짝 위로 (`translateY(8px) → 0`, opacity 0 → 1)
- 모달 등장: spring ease + scale 0.96 → 1
- 호버 시 그림자 강도 변화, 보더 컬러 변화
- 갤러리 그리드 stagger fade-in (각 30~50ms 차이)

**금지:**
- `transform: scale(>1.02)` 통통 호버
- `bounce` easing
- 자동 carousel
- 페이지 전환에 슬라이드 (Apple/Stripe는 페이드 위주)

### 3-7. 아이콘

- **이모지 추방 유지** (v2 그대로)
- **라인 아이콘 1.5~2px stroke.** Lucide React 또는 Phosphor Icons (light/regular 가중치)
- **아이콘 컬러는 단색** — 기본 `--text-secondary`, 호버 시 `--text-primary`
- **결정적인 카운터(80 / 1,247)는 아이콘 없이 숫자만**
- **CTA 안 아이콘은 16px**, 본문 옆 아이콘은 14px

---

## 4. 컬러 결정 워크플로

### 4-1. 컬러 사용 위계

| 단계 | 사용처 | 토큰 |
|------|--------|------|
| **0 — 배경 (90%)** | 메인 배경·여백·읽기 영역 | `--bg-base` (#FAFAF8) |
| **1 — 카드/패널 (8%)** | 카드·드롭다운·모달 내부 | `--bg-elevated` (#FFFFFF) |
| **2 — 호버/구분 (2%)** | 호버 배경·인풋·코드 블록 | `--bg-overlay`, `--bg-sunken` |
| **3 — 텍스트 (모노크롬)** | 본문·라벨·메타 | `--text-primary/secondary/tertiary` |
| **4 — 액센트 (한 페이지에 1~2 곳만)** | 메인 CTA·진행 중·중요 강조 | `--accent` |
| **5 — 상태색 (필요 시만)** | 오류·성공·경고 | `--critical`, `--success`, `--warning` |

**규칙:** 한 화면에 액센트는 최대 2개 영역. 그 외는 모노크롬으로.

### 4-2. 액센트 선택 — 확정

```
[ ] 안 A — Iris Violet #5B5BF7
[ ] 안 B — Cobalt #2563EB
[x] 안 C — Graphite #1A1A1B (모노크롬)

선택 사유: 사진이 주인공이 되도록 시스템 컬러를 최대한 양보.
          "Quiet Precision" 컨셉의 가장 충실한 구현.
선택일:   2026-05-14
적용 토큰:
  --accent:       #1A1A1B
  --accent-hover: #000000
  --accent-soft:  rgba(26, 26, 27, 0.10)
  --accent-quiet: rgba(26, 26, 27, 0.04)
```

상세 사양은 §3-2 참조. 상태 신호 보강(status 토큰 사용) 규칙도 §3-2에 정리되어 있음.

### 4-3. 다크 보조 사용 매트릭스

| 화면/컴포넌트 | 모드 | 이유 |
|---------------|------|------|
| Landing | 라이트 | 첫 인상·CTA 가독성 |
| TypeSelect / StudioTypeSelect | 라이트 | 선택 화면 — 정보 위주 |
| Upload / FolderUpload | 라이트 | 드롭존·진행 안내 |
| Analysis | 라이트 | 진행 상황·통계 표시 |
| PersonSelect | 라이트 | 인물 카드 그리드 — 작은 썸네일·정보 |
| **Gallery / FolderGallery** | **라이트 (디폴트) + 다크 토글** | **사진 비교 시 사용자 선택. 디폴트는 라이트로 가벼움 유지.** |
| **PhotoDetailModal (사진 한 장 전체 화면)** | **다크** | **사진 침범 최소화 — 사진 외 영역은 다크로 침묵.** |
| FullscreenViewer | 다크 | 사진 본래 톤 보존 |
| ZIP 진행 / 완료 | 라이트 | 작업 안내 |
| Onboarding / ReturningBanner | 라이트 | 카피 가독성 |
| Settings / Profile | 라이트 | 정보 위주 |

**구현 방법:** 다크 보조는 별도 모드 토글이 아니라, **해당 컴포넌트에 `data-theme="dark"` 속성을 두고 CSS 변수만 다크 셋으로 덮어쓰기**. 사용자가 다크/라이트를 의식적으로 전환하지 않음.

```css
[data-theme="dark"] {
  --bg-base:        var(--bg-base-dark);
  --bg-elevated:    var(--bg-elevated-dark);
  --bg-overlay:     var(--bg-overlay-dark);
  --text-primary:   var(--text-primary-dark);
  --text-secondary: var(--text-secondary-dark);
  --text-tertiary:  var(--text-tertiary-dark);
  --border-subtle:  var(--border-subtle-dark);
  --border-strong:  var(--border-strong-dark);
}
```

갤러리만 사용자 토글 노출:

```
[ 라이트 ]   다크
```

기본은 라이트, 사용자가 다크 선택 시 `localStorage.galleryTheme = "dark"`.

---

## 5. 보이스·카피 톤 — "차분한 어시스턴트"

### 5-1. 5가지 원칙 (v2.1 큐레이터 톤 폐기 후 재정의)

1. **명확하게.** 무엇을 할 수 있는지, 지금 무엇이 일어나고 있는지 직설적으로.
2. **자랑하지 않는다.** "✨ 완료!" → "완료" / "🎉 N장 다운로드!" → "N장 다운로드 시작"
3. **사용자를 어린이 취급하지 않는다.** "어서오세요 :)" / "잠시만 기다려주세요!" 같은 친절 과잉 카피 폐기.
4. **AI를 마법으로 포장하지 않는다.** "AI가 골라드려요" → "셀렉이 끝났습니다" 또는 "1,247장 중 80장 선택"
5. **숫자는 모노로, 자신 있게.** "약 80장" → "**80**장", "1~2분 소요" → "1분 23초 소요"

### 5-2. v2.1 큐레이터 톤 → v2.2 어시스턴트 톤 (주요 항목)

큐레이터 톤도 정리에는 효과적이었으나, "도록·로 끝나는 단정한 문장"이 라이트 + Apple/Stripe 톤과는 약간 어긋난다. 더 평이하고 명확하게.

| 위치 | v2.1 큐레이터 톤 | v2.2 어시스턴트 톤 |
|------|----------------|-------------------|
| 랜딩 헤드라인 | 1,500장의 컷에서, 80장의 결정. | 1,500장에서 베스트 80장. |
| 랜딩 서브 | 스튜디오 폴더 그대로. 손대지 않은 채. | 폴더 구조 그대로 ZIP으로 받아갑니다. |
| 메인 CTA | 사진을 끌어다 놓으세요 | 사진 또는 폴더 끌어다 놓기 |
| 분석 헤더 | 분석 중 | 분석 중 · 47% |
| 분석 누적 | 32 — 눈 감은 컷 | 눈 감은 컷 32 |
| 분석 마지막 한 줄 | "이벤트별로 베스트 컷을 분배하는 중입니다." (세리프 이탤릭) | 베스트 컷 정리 중 |
| 갤러리 헤더 | 80 SELECTED · 1,167 REJECTED (대문자 라벨) | 선택 80 · 제외 1,167 · 전체 1,247 |
| ZIP 무료 버튼 | 5장까지 무료 다운로드 | 5장 미리보기 (무료) |
| ZIP 유료 버튼 | 원본 ZIP · ₩2,900 | 원본 ZIP 다운로드 · ₩2,900 |
| 결제 후 토스트 | 결제 완료. ZIP 생성 중 · 47% | 결제 완료. ZIP 만드는 중 (47%) |
| 결과 안심 | 스튜디오로 보낼 준비가 되었습니다. | 스튜디오에 그대로 전달할 수 있습니다. |
| NOTES 빈 | NOTES 없음 — 흠잡을 데 없는 컷입니다. | 감점 사유 없음. |
| 같은 그룹 베스트 | 이 그룹의 최고점은 87점. | 같은 그룹의 다른 컷이 더 높은 점수입니다 (87점). [보기 →] |
| 30일권 | 30일 무제한 · ₩9,900 | 30일 무제한 · ₩9,900 (유지) |
| 완료 토스트 | 완료. | 다운로드 완료 |
| 재방문 배너 | 미완료 세션이 있습니다. 이어서 하시겠습니까? | 이전 작업이 남아 있습니다. 이어서 시작 |
| 온보딩 모달 | 환영합니다. | 딸깍픽스 시작하기 |

**규칙:**
- 마침표(.) 사용을 줄인다. 짧은 라벨은 마침표 없이 끝낸다.
- 명령형 또는 명사형으로 끝나는 짧은 라벨 (`다운로드 시작`, `다시 셀렉`, `폴더 추가`)
- 본문 한 단락은 마침표로 끝나도 OK. 라벨·버튼·토스트는 마침표 없음.

### 5-3. AI 마케팅 금지어 사전 (v2 그대로 유지)

다음 단어/표현은 코드·카피·메시지 어디서도 사용하지 않는다:

```
스마트 / 지능형 / 혁신적 / 마법 / Magic / Smart / Intelligent /
한 번에 / 놀라운 / 똑똑한 / 환상적 / 완벽한 / 최고의 /
딸깍 한 번에 / 뚝딱 / 알아서 / 자동으로 (자제, 가능하면 빼기) /
AI가 골라드려요 / AI가 분석 중 (주어 교체)
```

검증 명령:
```bash
grep -rnE "(스마트|지능형|혁신적|마법|Magic|Smart|Intelligent|한 번에|놀라운|똑똑한|환상적|완벽한|최고의|딸깍 한 번에|뚝딱|알아서|AI가 골라|AI가 분석)" src/messages/ src/components/
```

### 5-4. "AI" 단어 사용 한도

- **페이지당 최대 1회**, 가능하면 0회
- SEO 메타 태그(`title`, `description`)는 검색 의도 매칭 위해 1회 허용
- UI 본문/카피는 수동태 또는 주어 교체:

| Before | After |
|---|---|
| AI가 골라드려요 | 셀렉이 끝났습니다 |
| AI가 분석 중입니다 | 분석 중 |
| AI 보정 | 리터치 |
| AI 추천 | 추천 |
| 이 AI는 어떻게 작동하나요? | 작동 방식 |

### 5-5. 이름·라벨 미세 조정

- 앱명 표기: **`딸깍픽스`** (정자체) / 로고는 별도 디자인. v2.1의 `DDALGAK · PICKS` 미들닷 대문자 스타일 폐기 (도록 톤이 사라졌으므로).
- Flow 라벨: 사용자에게 "Flow A/B/C"는 노출하지 않는다. 내부 코드명만.
  - 사용자 표면: **〈사진 셀렉〉 / 〈스튜디오 셀렉〉**
- 탭: `선택 80` / `제외 1,167` / `전체 1,247` (한글 + 모노 숫자)
  - v2.1의 대문자 라벨(`SELECTED · 80`)은 폐기

---

## 6. 콘트라스트·접근성 보장 (사용자 통증 직접 대응)

> 사용자가 진단한 통증: *"네이비톤 배경에 검은 텍스트."* 이는 콘트라스트 부족 + 컬러 충돌이 겹친 문제. v2.2는 **모든 텍스트가 WCAG AA 이상**을 보장한다.

### 6-1. 콘트라스트 표

| 텍스트 | 배경 | 비율 | 등급 |
|--------|------|------|------|
| `--text-primary` #0A0A0B | `--bg-base` #FAFAF8 | **16.8:1** | AAA |
| `--text-primary` #0A0A0B | `--bg-elevated` #FFFFFF | **18.7:1** | AAA |
| `--text-secondary` #56565A | `--bg-base` #FAFAF8 | **7.2:1** | AAA |
| `--text-tertiary` #8A8A8E | `--bg-base` #FAFAF8 | **4.7:1** | AA |
| `--text-disabled` #C0C0BD | `--bg-base` #FAFAF8 | 2.1:1 | (의도적, 비활성 표시) |
| `--text-on-accent` #FFFFFF | `--accent` #1A1A1B (Graphite) | **18.7:1** | AAA |
| `--accent` #1A1A1B | `--bg-base` #FAFAF8 | **16.5:1** | AAA |
| `--critical` #D94B3D | `--bg-base` #FAFAF8 | **4.6:1** | AA |
| `--success` #2F8F5F | `--bg-base` #FAFAF8 | **4.5:1** | AA (경계선) |

### 6-2. 다크 보조 콘트라스트

| 텍스트 | 배경 | 비율 | 등급 |
|--------|------|------|------|
| `--text-primary-dark` #F4F4F5 | `--bg-base-dark` #0F0F10 | **17.2:1** | AAA |
| `--text-secondary-dark` #A8A8AC | `--bg-base-dark` #0F0F10 | **8.5:1** | AAA |
| `--text-tertiary-dark` #6D6D72 | `--bg-base-dark` #0F0F10 | **4.6:1** | AA |

### 6-3. 검증 도구

- 토큰 정의 시 [WebAIM Contrast Checker](https://webaim.org/resources/contrastchecker/) 또는 Figma/VS Code의 Stark 익스텐션으로 확인
- 신규 컬러 추가 시 본 §6-1 표에 행 추가 + 비율 측정 결과 기록
- 마이그레이션 PR 머지 전 다음 grep으로 잔존 하드코딩 확인:

```bash
# 다크 톤 잔존
grep -rnE "#(0D0F12|131720|1A1F28|2D4356|3E5670)" src/
# 큐레이터 톤 잔존
grep -rnE "(font-display|tracking-uppercase.*SELECTED|REJECTED)" src/
# 검정 텍스트 하드코딩
grep -rnE "color:\s*#000|color:\s*black" src/components/
```

---

## 7. 화면별 적용 가이드

### 7-1. 랜딩 (Landing.tsx)

**전체 톤:** 라이트, 큰 여백, 헤드라인 위주.

```
┌────────────────────────────────────────────────────────────┐
│                                              [GitHub] [?]  │  ← 우상단 아주 작게
│                                                            │
│                                                            │
│                                                            │
│              1,500장에서                                    │  ← Display-1, 800, -0.02em
│              베스트 80장.                                   │  ← Display-1, 800, --accent 색 강조
│                                                            │
│       폴더 구조 그대로 ZIP으로 받아갑니다.                    │  ← text-md, secondary, --space-6
│                                                            │
│                                                            │
│       ┌───────────────────────────────────────────┐        │
│       │                                           │        │
│       │         사진 또는 폴더 끌어다 놓기            │        │  ← 드롭존 카드, --bg-elevated,
│       │         또는 [폴더 선택] [Google Drive]      │        │     --radius-md, --shadow-sm,
│       │                                           │        │     bigger padding
│       └───────────────────────────────────────────┘        │
│                                                            │
│                                                            │
│       어떻게 작동하나요?                                     │  ← text-sm, secondary
│       1,000장 분석 → 흔들림·눈감음·중복 제거 →                 │
│       베스트 컷 선별 → 폴더 구조 ZIP                          │
│                                                            │
│                                                            │
│       지난 30일 124회 작업 · 평균 소요 4분                    │  ← text-xs, tertiary, mono 숫자
│                                                            │
└────────────────────────────────────────────────────────────┘
```

**핵심 변화 (v2.1 대비):**
- **2카드 분기는 v2.1과 같이 폐기.** 단일 드롭존, 자동 분기 (PHASE1_PLAN 3-1 단계 1).
- **헤드라인은 두 줄로, 두 번째 줄에 `--accent` 컬러 강조.** Apple 키노트 톤.
- **"AI Photo Selector · Beta" 뱃지·이모지 0건.**
- **드롭존은 카드처럼.** `--bg-elevated` 흰색 + 미세 그림자.
- **작동 방식 3단계 짧게.** "어떻게 작동하나요?" 호기심 자극 + 신뢰감.
- **재방문 통계는 아주 작게 푸터에.**

#### 헤드라인 후보 3안 (어시스턴트 톤)

```
1) 1,500장에서 베스트 80장.
   폴더 구조 그대로 ZIP으로 받아갑니다.

2) 사진 셀렉, 1,500장 → 80장.
   3분이면 끝납니다.

3) 좋은 사진만 추립니다.
   1,500장에서 80장. 폴더 그대로.
```

**추천: 1번** — 숫자가 명확하고, "베스트"가 핵심 가치.

### 7-2. 분석 화면

```
┌────────────────────────────────────────────────────────────┐
│                                                            │
│       분석 중                                  47%          │  ← 큰 헤드라인 + 모노 %
│       ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━              │     progress bar --accent
│                                                            │
│       ─────────────────────────────────────                │
│                                                            │
│       사진을 받았습니다                        1,247         │
│       그룹으로 묶었습니다                        124         │  ← 단정한 표, mono 우정렬,
│       눈 감은 컷                                  32         │     라벨 좌·숫자 우, --space-3
│       흔들린 컷                                   18         │
│       옆모습·뒷모습                               12         │
│                                                            │
│       ─────────────────────────────────────                │
│                                                            │
│                                                            │
│       베스트 컷 정리 중                                       │  ← text-sm, secondary
│                                                            │
└────────────────────────────────────────────────────────────┘
```

**핵심 변화:**
- 진행률 바: `--accent` 컬러, 높이 4px, radius 9999px.
- 단계별 카운터: 누적 fade-in, 30ms stagger.
- 마지막 한 줄은 작게(`text-sm`), `secondary` 컬러. 큐레이터 이탤릭 폐기.
- 이모지·느낌표 0개.

### 7-3. 갤러리 / 폴더 갤러리

**전체 톤:** 라이트 (디폴트) + 사용자 토글로 다크 옵션.

```
┌──────────────────────────────────────────────────────────────────┐
│ 딸깍픽스                만삭 / 베이비본 / 100일 / 돌       [라이트│다크]│
│ ────────────────────────────────────────────────────────────── │
│                                                                  │
│  ┌──────────────────┐                                            │
│  │                  │  탭 [선택 80] [제외 1,167] [전체 1,247]       │
│  │  선택       80   │  ─────                                     │
│  │   ─────          │                                            │
│  │  만삭       12   │  ┌───┬───┬───┬───┬───┐                     │
│  │  베이비본   23   │  │   │   │   │   │   │                     │
│  │  100일      18   │  │ 사│ 사│ 사│ 사│ 사│                     │  ← 사진 그리드,
│  │  돌         27   │  │ 진│ 진│ 진│ 진│ 진│                     │     5컬럼, 갭 4px,
│  │                  │  │   │   │   │   │   │                     │     --radius-photo: 4px
│  │  제외    1,167   │  └───┴───┴───┴───┴───┘                     │
│  │   ─────          │                                            │
│  │  눈 감음    32   │  ┌───┬───┬───┬───┬───┐                     │
│  │  흔들림     18   │  │   │   │   │   │   │                     │
│  │  중복      124   │  │ 사│ 사│ 사│ 사│ 사│                     │
│  │  저조도      7   │  │ 진│ 진│ 진│ 진│ 진│                     │
│  │  기타      986   │  │   │   │   │   │   │                     │
│  └──────────────────┘  └───┴───┴───┴───┴───┘                     │
│  좌측 메타 패널, sticky                                            │
│                                                                  │
│ ──────────────────────────────────────────────────────────────── │
│                                                                  │
│ 스튜디오에 그대로 전달할 수 있습니다                                   │
│ [원본 ZIP 다운로드 · ₩2,900]   [30일 무제한 · ₩9,900]                │
└──────────────────────────────────────────────────────────────────┘
```

**핵심 변화 (v2.1 대비):**
- 좌측 메타 패널 유지. 다만 v2.1의 큰 `--display-2` 숫자 대신 **본문 + 모노 숫자** 우정렬로 더 차분히.
- 사진 그리드: 갭 **4px** (v2.1의 1px → 4px로 약간 더 호흡), 모서리 **4px 미세 라운드** (v2.1 직각 폐기).
- 호버: 그림자 미세 추가 + 보더 `--accent`. scale·translate 금지.
- 결제 CTA: **버튼 2개 나란히 (메인은 `--accent` 배경, 보조는 `--bg-elevated` + 보더)**.
- 대문자 라벨(`SELECTED · 80`) 폐기 → 한글(`선택 80`).
- 갤러리 우상단 **[라이트│다크]** 미세 토글 — 사용자가 사진 비교 시 다크 모드 선택 가능. localStorage 저장.

### 7-4. 사진 상세 모달 (PhotoDetailModal — v2.2 마이그레이션 PR에서만 수정)

**전체 톤:** **다크.** 사진을 위해 시스템 UI는 침묵.

```
┌────────────────────────────────────────────────────────────┐
│  [×]                                          12 / 80      │  ← 다크 헤더, text-secondary-dark
│ ────────────────────────────────────────────────────────── │
│                                                            │
│                                                            │
│                                                            │
│        ┌────────────────────────────────────┐              │
│        │                                    │              │
│        │                                    │              │
│        │           [큰 사진]                  │              │  ← 사진은 4px 미세 라운드,
│        │                                    │              │     주변은 다크 침묵
│        │                                    │              │
│        └────────────────────────────────────┘              │
│                                                            │
│                                                            │
│       IMG_2871.jpg · f/1.4 · 1/200s · ISO 400              │  ← 모노 메타, text-tertiary-dark
│                                                            │
│       ─────────────────────────────────────                │
│                                                            │
│       점수                                  62 / 100        │  ← text-md, mono 숫자 강조
│                                                            │
│       감점 사유                                              │  ← text-sm, secondary-dark
│       눈 감음 · 왼쪽 인물                          −15        │
│       선명도 부족 · 약간의 흔들림                   −8        │
│                                                            │
│       같은 그룹의 다른 컷이 더 높은 점수입니다 (87점).            │
│       [그 컷 보기]                                          │
│                                                            │
│       ─────────────────────────────────────                │
│                                                            │
│       [← 이전]              [제외 ⌫]  [유지 ↵]     [다음 →] │  ← 키보드 단축키 표시
└────────────────────────────────────────────────────────────┘
```

**핵심 변화 (v2.1 대비):**
- v2.1의 "NOTES" 도록 라벨 → "감점 사유" 평이한 라벨.
- v2.1의 [빼기] / [유지] 텍스트 버튼 → **키보드 단축키 표시** (`⌫`, `↵`) 추가. Linear 톤.
- 다크 보조 적용 — `data-theme="dark"`.
- 사진은 4px 미세 라운드.

**⚠️ 코드 변경 시 `payment.ts`·`analyzer.ts`처럼 PhotoDetailModal 본체 수정은 마이그레이션 PR에서만.** AGENTS.md의 절대 규칙은 v2.2 마이그레이션 시점에 한해 PhotoDetailModal 허용으로 갱신 필요.

### 7-5. ZIP 진행 / 완료

```
[ZIP 진행]
┌────────────────────────────────────────────────────────────┐
│                                                            │
│       원본 ZIP 만드는 중                       47%          │
│       ━━━━━━━━━━━━━━━━━━━━━                                │
│                                                            │
│       80장 · 312 MB · 폴더 구조 보존                          │  ← text-sm, secondary, mono 숫자
│                                                            │
└────────────────────────────────────────────────────────────┘

[ZIP 완료]
┌────────────────────────────────────────────────────────────┐
│                                                            │
│       다운로드 완료                                          │  ← H2, primary
│                                                            │
│       원본 ZIP · 80장 · 312 MB                              │  ← text-md, secondary
│       [다시 받기]                                           │  ← 보조 버튼, --bg-elevated + 보더
│                                                            │
│       24시간 안에 다시 받을 수 있습니다                         │  ← text-sm, tertiary
│                                                            │
└────────────────────────────────────────────────────────────┘
```

v2.1의 "완료." 한 단어 폐기 → "다운로드 완료"로 자연스럽게.

---

## 8. 결정 갈림길에서 묻는 질문 (v2.2 갱신)

작업 중 결정이 애매할 때 다음 질문을 통과해야 한다:

1. **"이게 Apple/Arc/Stripe 같은가, 일반 SaaS 같은가?"** — Apple/Arc/Stripe 같으면 OK
2. **"라이트 베이스 위에 콘트라스트가 충분한가?"** — §6-1 표 기준 AA 이상
3. **"한 화면에 색이 5개 미만인가?"** — 액센트는 1, 회색 4, 상태 0~2
4. **"호버 시 위치가 움직이지 않는가?"** — scale·translateY 금지
5. **"숫자가 모노로, 정렬되어 있는가?"** — 라벨은 좌, 숫자는 우정렬 + 모노
6. **"이모지·느낌표가 있는가?"** — 있으면 빼기
7. **"이게 도구인가, 마법사인가?"** — 도구면 OK
8. **"AI라는 단어가 페이지에 두 번 이상 보이는가?"** — 한 번 빼기
9. **"모바일에서 가로 스크롤이 발생하는가?"** — `overflow-x: hidden` + max-width
10. **"사진이 주인공인가, 시스템 UI가 주인공인가?"** — 사진이 주인공이면 OK

10가지가 모두 통과하면 v2.2 톤에 부합한다.

---

## 9. 모바일 가이드 (v2 §10 그대로 유지)

브레이크포인트·터치 타겟·iOS Safari 특화 사항은 v2 §10이 여전히 유효. 변경 사항은 다음만:

```css
/* 모바일 타이포 — v2.2 스케일에 맞춰 갱신 */
@media (max-width: 768px) {
  :root {
    --display-1: 40px;
    --display-2: 56px;
    --text-2xl:  28px;
    --text-xl:   24px;
    --text-base: 16px;   /* 모바일 가독성 */
    --space-24:  48px;
    --space-32:  64px;
  }
}
```

- **랜딩의 단일 드롭존**은 모바일에서 사진앨범 시트 폴백 (`accept="image/*"`)
- **갤러리 그리드:** 데스크톱 5컬럼 → 태블릿 3 → 모바일 2 (4:5 인물 친화)
- **사진 상세 모달:** 데스크톱 가운데 모달 → 모바일 풀스크린 슬라이드업 (다크)
- **모바일에서 "안 되는 것":** 폴더 통째 드래그앤드롭·다중 폴더·AlbumContainer (v2 §10-6 그대로)

---

## 10. 마이그레이션 우선순위

> **본 v2.2 문서는 시각·카피 사양만 정의한다. 코드 마이그레이션은 별도 PR.** 다음은 마이그레이션 시 우선순위 매트릭스.

### 10-0. 작업 순서 결정 (2026-05-14)

```
v0.5.4-stable (현재 main)
       │
       ▼
v0.5.5 — 디자인 마이그레이션 (이 문서 §10-1 적용)   ← 우선
       │
       ▼
v0.6.0 — 소셜 로그인 (CLAUDE_CODE_PROMPT_v0.5.5_SOCIAL_LOGIN_PREP.md)
       │
       ▼
v0.6.1+ — 결제 활성화 / 점진 적용 (§10-2)
```

**결정 사유:**
1. **소셜 로그인 UI가 신 디자인 시스템 위에 얹혀야 한다.** 큐레이터 톤 + 네이비 베이스 위에 OAuth 버튼을 추가하면, 곧이어 디자인 마이그레이션 PR에서 다시 갈아엎어야 함. 작업 중복.
2. **사용자 통증(콘트라스트·정리되지 않은 느낌) 우선 해소.** 베타 사용자가 늘기 전에 첫인상 톤을 잡는 것이 회원 전환에 더 유리.
3. **소셜 로그인은 게스트 모드 호환을 유지해야 하므로 긴급도가 낮다.** 익명 사용 흐름이 이미 동작 중.

**버전 번호 갱신:** 기존 인계 문서의 "v0.5.5 = 소셜 로그인"은 **v0.6.0으로 밀린다.** 본 디자인 마이그레이션 PR이 v0.5.5를 차지.

### 10-1. v0.5.5 — 디자인 마이그레이션 메인 PR (소셜 로그인 이전)

| 순위 | 항목 | 비용 | 영향 |
|-----|------|------|------|
| 1 | `tokens.css` 전면 교체 (라이트 베이스) | 작음 | 전체 화면 톤 한 번에 전환 |
| 1 | `--accent` 사용자 결정 → 토큰 확정 | 작음 | 결제 CTA·진행률 신호 |
| 1 | 큐레이터 톤 카피 → 어시스턴트 톤 (§5-2 표 전체 적용, `messages/ko.json`) | 중 | 전 화면 카피 |
| 2 | Landing.tsx 헤드라인 + 단일 드롭존 | 중 | 첫 인상 |
| 2 | 갤러리 좌측 메타 패널 본문+모노 차분화 | 작음 | 가장 많이 보는 화면 |
| 2 | 사진 그리드 갭 4px + 미세 라운드 4px | 작음 | 시각 톤 |
| 3 | 다크 보조 — PhotoDetailModal `data-theme="dark"` | 중 | 사진 침범 최소화 |
| 3 | 갤러리 [라이트│다크] 토글 + localStorage | 작음 | 사용자 선택권 |
| 4 | WCAG AA 검증 — 잔존 하드코딩 텍스트·배경 grep 정리 | 작음 | 콘트라스트 통증 해소 |
| 4 | 모션 ease 교체 (`cubic-bezier(0.32, 0.72, 0, 1)`) | 작음 | 톤 |
| 4 | 모바일 타이포 스케일 갱신 | 작음 | 모바일 가독성 |
| 5 | `--font-display` 토큰 제거 + 잔존 grep | 작음 | 정리 |
| 5 | 형광 그린·차가운 네이비 잔존 그렙 | 작음 | 정리 |
| 5 | globals.css 별칭 블록 제거 (v0.5.1부터 유지된 숙제) | 중 | 토큰 일원화 |

### 10-2. v0.6.1 이후 (점진 적용 — 소셜 로그인 v0.6.0 이후)

- 누적 통계 푸터 (랜딩 하단·결과 화면 끝)
- 단축키 시스템 (PhotoDetailModal `←/→/⌫/↵` + 갤러리 `j/k`)
- 모바일 사진 모달 슬라이드업 시트 폴백
- 사진 비교 모달 (GroupCompareModal) 다크 보조 적용
- 소셜 로그인 UI 자체는 v0.6.0에서 본 v2.2 가이드를 따라 신규 설계

### 10-3. 검증 명령 (v0.5.5 마이그레이션 PR 머지 전)

```bash
# 1. 이전 다크 톤 잔존
grep -rnE "#(0D0F12|131720|1A1F28|2D4356|3E5670|EDF0F5|9FAABF|6B7A94)" src/

# 2. 큐레이터 톤 잔존
grep -rnE "(NOTES|SELECTED|REJECTED|font-display|tracking-uppercase)" src/components/

# 3. 이모지·느낌표 잔존
grep -rnE "[📷📁📦🎯💎📌👋🎉🔁🔒✨🪄✦]" src/components/ src/messages/

# 4. AI 마케팅 금지어
grep -rnE "(스마트|지능형|혁신적|마법|Magic|Smart|Intelligent|한 번에|놀라운|똑똑한|환상적|완벽한|최고의|딸깍 한 번에|뚝딱|알아서|AI가 골라|AI가 분석)" src/messages/ src/components/

# 5. hover transform
grep -rn "transform.*translateY\|transform.*scale" src/components/

# 6. 검정 텍스트 하드코딩
grep -rnE "color:\s*#000(000)?\b|color:\s*black" src/components/

# 7. 콘트라스트 — 의심되는 회색 텍스트
grep -rnE "color:\s*#[6-9a-fA-F][0-9a-fA-F]{5}" src/components/ | grep -v "var(--"
```

위 7개 grep이 모두 출력 0(또는 의도된 결과)이 되어야 v0.5.5 머지 가능.

---

## 11. PHASE1_PLAN과의 관계

- **기능 스펙**(어떤 기능을 만들지)은 `PHASE1_PLAN.md` 그대로 유효
- **시각 언어·카피·톤**(어떻게 보일지)는 이 v2.2 문서가 우선
- v2.1 큐레이터 톤으로 작성된 PHASE1_PLAN의 카피 예시는 점진적으로 §5-2 어시스턴트 톤으로 교체
- 사진 상세 모달의 NOTES 사양(§4-4 v2.1)은 §7-4 v2.2 사양으로 대체

---

## 12. v2.2 적용 원칙

본 문서는 코드 변경 없이 **사양만 먼저 잡는다.** 다음 PR(v0.5.5)에서 §10-1 매트릭스 순서대로 마이그레이션 진행 — 소셜 로그인(v0.6.0)보다 먼저. 그 전까지 모든 시각·카피 결정은 이 문서를 기준으로 한다.

새 화면을 만들 때마다 §8의 10가지 질문을 통과해야 한다. 통과하지 못한 항목은 PR 디스크립션에 "v2.2 가이드 위반 — 사유" 형태로 명시.

---

## 부록 A — v2.2 변경 요약 (v2.1 → v2.2 한눈에)

| 영역 | v2.1 | v2.2 |
|------|------|------|
| **컨셉** | "다크룸의 큐레이터" | "Quiet Precision" |
| **배경** | 다크 디폴트 (`#0D0F12` 차가운 네이비) | **라이트 메인 + 다크 보조** (`#FAFAF8` 따뜻한 오프화이트) |
| **레퍼런스** | Lightroom·Magnum·MoMA·박물관 도록 | **Apple·Arc·Stripe** |
| **헤드라인 폰트** | Pretendard 800 (v2.1에서 세리프 폐기) | Pretendard 700~800 (유지) |
| **액센트** | 잉크 블루 `#2D4356` | **Graphite `#1A1A1B` (모노크롬, 2026-05-14 확정)** |
| **사진 모서리** | 직각 (`--radius-photo: 0`) | **4px 미세 라운드** |
| **카드/버튼 라운드** | 4px | **8px** |
| **카드 호버** | 보더만 (그림자 금지) | **그림자 1단계 + 보더 강화** (Apple 톤) |
| **모션 ease** | `cubic-bezier(0.4, 0.0, 0.2, 1)` | **`cubic-bezier(0.32, 0.72, 0, 1)` (Apple-style)** |
| **카피 톤** | 큐레이터 (도록 캡션·"완료.") | **차분한 어시스턴트** ("다운로드 완료") |
| **NOTES** | 도록 NOTES 라벨 | **"감점 사유"** 평이한 라벨 |
| **갤러리 라벨** | `SELECTED · 80` 대문자 트래킹 | **`선택 80`** 한글 + 모노 숫자 |
| **앱명 표기** | `DDALGAK · PICKS` | **`딸깍픽스`** |
| **다크 모드** | 디폴트·유일 | **갤러리에 한해 사용자 토글** |
| **콘트라스트** | 부분적으로 보장 | **§6-1 표로 전수 보장 (WCAG AA 이상)** |

---

> **이 문서는 살아있는 문서다.** 마이그레이션 PR 진행 중 발견되는 이슈는 §부록 B(향후 추가)에 누적 기록. 두 번째 큰 결정이 필요해지면 v2.3으로 발간.
