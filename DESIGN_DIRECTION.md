# 딸깍픽스 디자인 방향: "다크룸의 큐레이터"

> **작성일:** 2026-04-29
> **상위 문서:** `PROJECT_STATUS.md`, `PHASE1_PLAN.md`
> **이 문서의 목적:** 제품의 시각 언어·보이스·UI 톤을 "전문가가 큐레이팅해준 사진" 감각으로 통일한다. Phase 1 작업과 병행해 점진 적용한다.

---

## 0. 한 문장 컨셉

> **"AI 도구"가 아니라 "디지털 큐레이터"처럼 보이게.**
>
> 사용자는 자신이 사진을 고른 게 아니라, **"안목 있는 전문가가 골라준 결과를 검토하는 중"**이라고 느껴야 한다.

이 한 줄이 모든 시각·카피 결정의 기준이다. 갈림길마다 묻는다: *"이게 라이트룸 같은가, 카카오톡 같은가?"*

---

## 1. 진단 — 지금이 왜 일반 웹사이트처럼 보이는가

| 영역 | 현재 (추정) | 인식되는 인상 |
|------|------------|--------------|
| 색감 | 라이트 모드 + 브랜드 컬러 | "또 하나의 SaaS" |
| 타이포 | 시스템 산세리프 (Pretendard 등) 균일 | 정보 전달 OK, 권위감 없음 |
| 카피 | 이모지(✨🪄📦), 느낌표, "어서오세요 :)" | 친근하지만 가벼움 — "AI 친구" |
| CTA | 그라데이션 버튼 + 둥근 모서리 + 큰 그림자 | 결제 유도 SaaS 톤 |
| 레이아웃 | 카드 그리드, 균일한 패딩 | "기능 나열형 랜딩" |
| 모션 | 통통 튀는 호버, 빠른 트랜지션 | 가벼움 |
| 아이콘 | 이모지 + Lucide stock | 일관성 없음 |

**결론:** 시각·언어·인터랙션이 모두 "**친절함**"에 맞춰져 있어서 "**전문성**"이 들어설 자리가 없다. 친절함 ≠ 신뢰감. 큐레이터는 침착하고, 단정하고, 자기 주장이 있다.

---

## 2. 무드 레퍼런스 (벤치마킹할 곳 / 피해야 할 곳)

### 2-1. 닮을 곳

| 레퍼런스 | 차용할 요소 |
|----------|------------|
| **Adobe Lightroom · Capture One** | 다크 인터페이스, 사진을 절대주인공으로 두는 UI 크롬 절제 |
| **Aftershoot, Narrative Select** | 사진 셀렉 전문툴의 정보 밀도와 단정함 |
| **Magnum Photos · Vii Photo** | 사진 에이전시의 흑백·세리프·여백 사용 |
| **The New Yorker Photo Essays** | 에디토리얼 타이포, 캡션 스타일 |
| **MoMA · Tate Modern 웹사이트** | 갤러리/도록의 권위감, 큰 여백, 작은 본문 |
| **Are.na, Cosmos.so** | 큐레이션 자체가 콘텐츠인 인터페이스 |
| **Linear, Things 3** | 다크모드 디테일, 모션 절제, 단축키 우대 |

### 2-2. 피할 것

- **Notion·Slack의 카드 + 그라데이션 톤** — SaaS 친화 톤
- **Duolingo·Toss의 컬러풀 일러스트** — 친근함이 권위감을 잡아먹음
- **이모지 헤더** — 한 번에 톤이 무너짐
- **Airbnb식 둥근 카드 + 그림자** — "친절한 마켓플레이스" 인상

---

## 3. 비주얼 토큰 (CSS 변수로 바로 적용 가능)

### 3-1. 컬러 — 절제된 모노크롬 + 단일 액센트

```css
:root {
  /* Surface — 라이트룸 다크 톤. 순수 #000은 피한다(피로). */
  --bg-base:        #0E0D0B;   /* 메인 배경, 약간의 따뜻한 기 */
  --bg-elevated:    #16140F;   /* 카드/패널 */
  --bg-overlay:     #1D1A14;   /* 모달, 호버 */

  /* Borders — 거의 안 보이게 */
  --border-subtle:  #26221A;
  --border-strong:  #3A3328;

  /* Text — 따뜻한 오프화이트 (#FFF는 너무 차갑다) */
  --text-primary:   #F2EDE3;   /* 본문 */
  --text-secondary: #A39B89;   /* 보조 정보, 캡션 */
  --text-tertiary:  #6B6555;   /* 메타데이터, 타임스탬프 */
  --text-disabled:  #443F35;

  /* 단일 액센트 — 따뜻한 황동/구리 톤 (브랜드를 박지 않는다) */
  --accent:         #C9A961;   /* 박물관 골드 */
  --accent-soft:    #C9A96120; /* 12% 투명 — 호버 배경 등 */

  /* Critical — 옥스블러드. 형광 빨강 금지. */
  --critical:       #B23B2A;
  --warning:        #C8923D;   /* 액센트와 통일감 */

  /* Photo metadata — 사진 위에 얹는 정보용 */
  --photo-overlay:  rgba(14, 13, 11, 0.72);
}

/* 라이트 모드는 Phase 1에서 만들지 않는다. 다크가 디폴트이자 유일한 모드. */
```

**핵심 원칙:** 액센트 컬러는 **결제 CTA·진행 중인 상태·중요 강조**에만. 일반 버튼은 모노크롬으로.

### 3-2. 타이포그래피 — 세리프 디스플레이 + 정제된 산세리프

```css
:root {
  /* 헤드라인·도록 제목 — 세리프 (한국어는 명조 계열) */
  --font-display:
    "Spectral", "Noto Serif KR", "Nanum Myeongjo",
    Georgia, serif;

  /* 본문·UI — 산세리프 (Pretendard 유지하되 더 작게, 더 단정하게) */
  --font-sans:
    "Pretendard Variable", "Pretendard",
    -apple-system, BlinkMacSystemFont, sans-serif;

  /* 숫자·메타데이터 — 모노. 카운트·% 표시할 때 정렬감 */
  --font-mono:
    "JetBrains Mono", "IBM Plex Mono", "SF Mono", monospace;

  /* 스케일 — 본문은 더 작게, 헤드라인은 더 크게 (콘트라스트 강화) */
  --text-xs:   12px;   /* 메타, 캡션 */
  --text-sm:   13px;   /* UI 라벨 */
  --text-base: 15px;   /* 본문 */
  --text-md:   18px;   /* 부제 */
  --text-lg:   24px;
  --text-xl:   36px;   /* 화면 헤드 */
  --display-1: 56px;   /* 랜딩 메인 */
  --display-2: 80px;   /* 갤러리 카운터 ("80") */

  /* 행간·자간 */
  --leading-tight: 1.1;   /* 헤드라인 */
  --leading-base:  1.55;  /* 본문 */
  --tracking-display: -0.02em; /* 세리프 헤드는 살짝 좁게 */
  --tracking-uppercase: 0.12em; /* 라벨용 대문자 */
}
```

**적용 규칙:**
- **세리프**는 화면 헤더·갤러리 카운터·결과 요약 1줄에만. 어디나 쓰면 무겁다.
- **모노**는 숫자(`1,247 → 80`, `47%`, `₩2,900`)에. 사진 전문툴의 정보 밀도 시그널.
- **대문자 트래킹** 라벨은 섹션 헤더에 (`SELECTED · 80`, `REJECTED · 1,167`).

### 3-3. 공간 — 여백을 두려워하지 않기

```css
:root {
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-6: 24px;
  --space-8: 32px;
  --space-12: 48px;
  --space-16: 64px;
  --space-24: 96px;   /* 섹션 간 여백 — 갤러리는 이 정도 시원하게 */
  --space-32: 128px;
}
```

**원칙:** 카드 그림자/보더 대신 **여백으로** 구분한다. UI 크롬은 줄이고 콘텐츠를 띄운다.

### 3-4. 모서리·그림자 — 거의 없게

```css
:root {
  --radius-sm:  2px;   /* 버튼·인풋 — 통통 튀지 않게 */
  --radius-md:  4px;   /* 카드 */
  --radius-lg:  8px;   /* 모달 */
  --radius-photo: 0;   /* 사진은 절대 둥글게 자르지 않는다 */

  --shadow-none: none;
  --shadow-modal: 0 24px 64px rgba(0, 0, 0, 0.5);
  /* 카드 그림자는 사용하지 않는다. 보더 + 여백으로 대체. */
}
```

**핵심:** 사진은 **각져야 한다.** 사진을 둥글게 깎으면 폴라로이드 인스타 톤이 된다. 갤러리·도록 톤을 원하면 직각.

### 3-5. 모션 — 절제 (튀지 않게)

```css
:root {
  --ease-standard: cubic-bezier(0.4, 0.0, 0.2, 1);
  --ease-decel:    cubic-bezier(0.0, 0.0, 0.2, 1);

  --dur-fast:   120ms;  /* 호버 */
  --dur-base:   240ms;  /* 모달, 탭 전환 */
  --dur-slow:   480ms;  /* 페이지 전환, 큰 reveal */
}
```

**금지:**
- `transform: scale(1.05)` 호버 통통 효과 → 대신 `opacity` 또는 보더 변경만
- `bounce` easing → 큐레이터는 통통 튀지 않는다
- 자동 carousel → 정지된 갤러리

**권장:**
- 페이드 인 + 살짝 위로 (`translateY(8px) → 0`)
- 갤러리 그리드는 **순차적으로 fade-in** (stagger 30ms씩)

### 3-6. 아이콘 — 이모지 추방, 라인 아이콘 통일

- 이모지(✨🪄📦) 전부 **제거**
- 통일된 라인 아이콘 셋만 사용 — Lucide의 `stroke-width: 1.5` 톤 또는 `Phosphor Icons`의 *thin/light* 가중치
- 아이콘은 **단색 (var(--text-secondary))**, 호버 시 var(--text-primary)
- 결정적인 카운터(80 / 1,247)는 아이콘 없이 **숫자만**

---

## 4. 화면별 적용 가이드

### 4-1. 랜딩

#### Before (현재 추정)

```
[2개 컬러풀 카드 그리드, 그라데이션 CTA, 이모지 ✨]
"사진 1,500장, 10분 만에 셀렉 끝."
[사진만 셀렉] [스튜디오용 셀렉]
```

#### After (큐레이터 모드)

```
─────────────────────────────────────────
                    DDALGAK · PICKS
                 사진 셀렉 큐레이터

       (큰 여백)


    1,500장의 컷에서,
    80장의 결정.        ← 세리프 디스플레이, --display-1


    스튜디오 폴더 그대로. 손대지 않은 채.   ← 본문, --text-md, secondary


    ─────────────────────────────

    [   사진을 끌어다 놓으세요   ]
    또는 [폴더 선택] · [Google Drive]

    ─────────────────────────────

       (큰 여백)


    지난 30일 동안 1,847장에서 124장을 골랐습니다.
                                    ← 메타 정보, 모노, --text-tertiary

─────────────────────────────────────────
```

**핵심 변화:**
- "사진만/스튜디오용" 카드 분기를 **랜딩에서 제거**. 드롭 후 시스템이 추천 (PHASE1_PLAN 3-1과 동일 방향, 단계 1의 자동분기 적용).
- 헤드라인은 **세리프 + 두 줄로 끊어** 도록 캡션처럼.
- "DDALGAK · PICKS" 워드마크는 작게, 대문자 트래킹.
- 재방문자 통계는 **자랑하지 않고 조용히** 하단에. 이게 신뢰감을 만든다.

#### 헤드라인 후보 3안 (모두 큐레이터 톤)

```
1) 1,500장의 컷에서, 80장의 결정.
   스튜디오 폴더 그대로. 손대지 않은 채.

2) 가장 어려운 일은, 빼는 것입니다.
   1,247장에서 80장. 폴더 구조는 그대로.

3) 셀렉을 큐레이터에게.
   당신은 마지막 한 번만 확인하세요.
```

**권장: 1번** — "결정"이라는 단어가 셀렉의 본질을 이름 짓는다. 숫자는 모노로, "결정"은 세리프로 강조.

### 4-2. 분석 화면

#### Before

```
[큰 진행률 바 70%]
"눈 감은 컷 N장 발견 ✨"
```

#### After

```
─────────────────────────────────────────

    분석 중                    47%
                          ━━━━━━━━━━━━━

    ─────────────────────────────


    1,247           사진을 받았습니다
    124             그룹으로 묶었습니다
    32              눈 감은 컷
    18              흔들린 컷
    12              옆모습·뒷모습

    ─────────────────────────────


    "이벤트별로 베스트 컷을 분배하는 중입니다."
                                  ← 세리프 이탤릭, secondary

─────────────────────────────────────────
```

**핵심 변화:**
- 진행 사항을 **단정한 표 형식**으로. 모노 숫자 좌측 + 라벨 우측.
- 발견 항목이 **누적되어 나타남** (PHASE1_PLAN 1-1 그대로, 단 카피 톤만 교체).
- 마지막 한 줄만 세리프 이탤릭으로 — *"분배하는 중입니다."* 도록 캡션 톤.
- 이모지·느낌표 0개.

### 4-3. 갤러리 (가장 많이 보는 화면)

#### Before

```
[사진 그리드, 우측에 "✓ 80장 선택됨" 카운터]
[하단 sticky bar: ✨ ZIP 다운로드 ₩2,900]
```

#### After

```
─────────────────────────────────────────
DDALGAK · PICKS                만삭 / 베이비본 / 100일 / 돌
─────────────────────────────────────────

      80                    SELECTED
                            만삭 폴더 · 12장
   ─────                    베이비본 폴더 · 23장
                            100일 폴더 · 18장
                            돌 폴더 · 27장

   1,167                    REJECTED
                            눈 감음 · 32
                            흔들림 · 18
                            중복 · 124
                            기타 · 993

      0                     PENDING

─────────────────────────────────────────

  [ Selected · 80 ]   Rejected · 1,167   All · 1,247
  ─────────────────

  [사진 그리드 — 직각, 균일 갭 1px, 호버 시 보더만 액센트로]


─────────────────────────────────────────
                                          ↓
  스튜디오로 보낼 준비가 되었습니다.

  [원본 ZIP · ₩2,900]   30일 무제한 · ₩9,900
─────────────────────────────────────────
```

**핵심 변화:**
- 좌측에 **메타 패널** (셀렉 통계). 큐레이터의 "캡션 카드"처럼.
- **숫자가 디스플레이급** (--display-2, 80px 이상). 카운터를 자랑한다.
- 사진 그리드는 **갭 1px, 직각**. 갤러리 벽처럼 빽빽이.
- 호버 효과는 **보더 1px solid var(--accent)** — scale·shadow 없음.
- 결제 CTA 위 한 줄: *"스튜디오로 보낼 준비가 되었습니다."* 세리프, 큐레이터의 사인.

### 4-4. 사진 상세 모달 (PHASE1_PLAN ⑥와 결합)

```
─────────────────────────────────────────
       [← 이전]    IMG_2871 · 12 of 80    [다음 →]

       ┌───────────────────────────────┐
       │                               │
       │      [큰 사진, 직각, 1:1]      │
       │                               │
       └───────────────────────────────┘

       ─────────────────────────────────


       SCORE                       62 / 100
       ─────

       NOTES
       눈 감음          왼쪽 인물       −15
       선명도 부족      약간의 흔들림    −8
       정면              ✓               0


       이 그룹의 최고점은 87점.
       [그 컷 보기 →]

       ─────────────────────────────────

       [ 빼기 ]                     [ 유지 ]
─────────────────────────────────────────
```

**핵심 변화:**
- "감점 사유"를 **NOTES 섹션**으로 명명. 큐레이터가 작품에 다는 노트 톤.
- 점수 표기는 모노 + 큰 디스플레이. 정성껏 매긴 점수처럼.
- "더 좋은 컷이 있어요!" → *"이 그룹의 최고점은 87점."* 차분한 사실 진술.
- 결정 버튼은 **테두리만 있는 텍스트 버튼** [ 빼기 ] / [ 유지 ]. 크고 단순하게.

### 4-5. ZIP 진행 / 결제

```
─────────────────────────────────────────

    원본 ZIP을 만들고 있습니다           47%
                                         ━━━━━━━━━━━━

    80장 · 312 MB · 폴더 구조 보존

─────────────────────────────────────────
```

```
─────────────────────────────────────────

    완료.

    원본 ZIP · 80장 · 312 MB
    [ 다시 받기 ]

    24시간 안에 다시 받으실 수 있습니다.

─────────────────────────────────────────
```

"완료." 한 단어로 끝. 큐레이터는 자축하지 않는다.

---

## 5. 보이스 가이드 — "큐레이터의 일지"

### 5-1. 5가지 원칙

1. **선언형으로.** "~해드릴게요" → "~합니다." / "~했어요!" → "~했습니다."
2. **이모지·느낌표 금지.** 신뢰감을 깎는다. 강조는 타이포·여백으로.
3. **숫자를 자신 있게.** "80장 정도" → "**80장**". 모호함은 권위를 죽인다.
4. **사진 전문 용어 쓰기.** "고르기" → "셀렉" / "에디트", "사진" → "컷", "보내기" → "프린트 / 전달"
5. **자축하지 않기.** "✨ 완료!" → "완료." / "🎉 ZIP 받기" → "원본 ZIP"

### 5-2. 카피 리라이트 표 (PHASE1_PLAN 충돌 해소 포함)

| 위치 | Before (친근 톤) | After (큐레이터 톤) |
|------|-----------------|--------------------|
| 랜딩 헤드라인 | 사진 1,500장, 10분 만에 셀렉 끝. | 1,500장의 컷에서, 80장의 결정. |
| 랜딩 서브 | 스튜디오 폴더 그대로, 베스트 컷만 ZIP으로 받아가세요. | 스튜디오 폴더 그대로. 손대지 않은 채. |
| 메인 CTA | 지금 폴더 끌어다 놓기 | 사진을 끌어다 놓으세요 |
| 분석 시작 | 🪄 셀렉 시작하기 | 셀렉 시작 |
| 분석 헤더 | 사진 1,247장 받았어요. 잠시만요... | 분석 중 |
| 분석 누적1 | 눈 감은 컷 32장 발견 | 32 — 눈 감은 컷 |
| 분석 마지막 | ✨ 셀렉 완료! 1,247장 → 80장으로 추렸어요 | 1,247장에서 80장. |
| 갤러리 헤더 | ✨ N장 → M장으로 추렸어요 | 80 SELECTED · 1,167 REJECTED |
| ZIP 무료 버튼 | 📦 5장까지 무료 받기 | 5장까지 무료 다운로드 |
| ZIP 유료 버튼 | 📦 폴더 구조 그대로 ZIP 받기 · ₩2,900 | 원본 ZIP · ₩2,900 |
| 결제 후 토스트 | ✅ 결제 완료! ZIP 만드는 중... 47% | 결제 완료. ZIP 생성 중 · 47% |
| 결과 안심 1 | 스튜디오에 그대로 보내셔도 돼요. | 스튜디오로 보낼 준비가 되었습니다. |
| 결과 안심 2 | 마음에 안 드는 컷은 클릭해서 빼세요. | 컷을 클릭하면 상세를 확인할 수 있습니다. |
| 재추출 | 🎯 내 취향대로 다시 고르기 | 다시 셀렉 |
| 취향 자동 적용 | 📌 지난 돌잔치 셀렉 취향을 적용했어요 | 지난 〈돌잔치〉 셀렉 기준을 적용했습니다 |
| 온보딩 모달 | 어서오세요 :) | 환영합니다. |
| 재방문 배너 | 👋 다시 오셨네요. 지난번 셀렉 이어서 하시겠어요? | 미완료 세션이 있습니다. 이어서 하시겠습니까? |
| 빈 감점 사유 | ✨ 흠잡을 데 없는 컷이에요 | NOTES 없음 — 흠잡을 데 없는 컷입니다. |
| 같은 그룹 베스트 | 같은 그룹에서 더 좋은 컷이 선택됐어요 | 이 그룹의 최고점은 87점. |
| 30일권 | 💎 30일 무제한권 · ₩9,900 | 30일 무제한 · ₩9,900 |
| 완료 토스트 | 🎉 다운로드 시작! | 완료. |

**규칙:** 위 표는 "이 톤이 더 낫다"의 예시지, 절대 규칙이 아니다. **이모지와 느낌표만은 무조건 빼라**. 그것만 지키면 80%는 큐레이터 톤에 가까워진다.

### 5-3. 이름·라벨 미세 조정

- 앱명 표기: `딸깍픽스` 또는 `DDALGAK · PICKS` (대문자 + 미들닷). 이미지·도록 톤.
- Flow 라벨: 사용자에게 "Flow A/B/C"는 노출하지 않는다. 내부 코드명만.
  - 사용자 표면: **〈사진 셀렉〉 / 〈스튜디오 셀렉〉 / 〈앨범 배치〉**
- 탭: `Selected · 80` / `Rejected · 1,167` / `All · 1,247`
  - 한국어로 가도 됨: `선택 · 80` / `제외 · 1,167` / `전체 · 1,247`
  - 단, 한글일 때도 **모노 숫자 + 미들닷 구분**은 유지

### 5-4. "AI" 단어 사용 가이드

- AI라는 단어를 자주 쓰면 **"기계"** 인상이 강해진다. 큐레이터 톤에서는 **빼거나 한 번만**.
- "AI가 골라드려요" → "**셀렉이 끝났습니다.**" (주체를 모호하게)
- "AI 보정" → "리터치" 또는 "에디트"
- "딸깍픽스의 AI는..." → "딸깍픽스는 흔들림·눈감음·중복을 먼저 골라냅니다." (제품이 주어)

---

## 6. 구현 우선순위 (실제 작업 순서)

### Step 1 — 토큰 정리 + 기본 다크 모드 (최우선, 1~2일)

```bash
# 작업 단위
src/styles/tokens.css   # 위 §3의 CSS 변수 전체 정의
src/styles/global.css   # body { background: var(--bg-base); color: var(--text-primary); }
                        # font-family 적용
```

**성공 기준:** 모든 화면이 다크 + 따뜻한 오프화이트로 통일. 기능 그대로.

### Step 2 — 이모지·느낌표 일괄 제거 + 카피 리라이트 (1일)

`src/messages/ko.json` 전체 그렙으로 정리:
```bash
# 점검 명령
grep -n "[✨🪄📦🎯💎📌👋🎉🔁🔒]" src/messages/ko.json
grep -n "!" src/messages/ko.json
```

§5-2 표 기준으로 일괄 교체. 가장 작은 비용 / 가장 큰 톤 전환.

### Step 3 — 타이포 시스템 적용 (1~2일)

- 세리프 웹폰트 로드 (Spectral 또는 Noto Serif KR)
- 헤드라인·갤러리 카운터·결과 한 줄에만 적용
- 모노 폰트(JetBrains Mono)는 숫자 표시 컴포넌트(`<MonoNumber />`)로 분리

### Step 4 — 갤러리·모달 레이아웃 재설계 (3~5일)

- 좌측 메타 패널 신설 (Selected/Rejected/Pending 큰 숫자)
- 사진 그리드 갭 1px, 직각, 호버=보더만
- 사진 상세 모달은 PHASE1_PLAN ⑥와 함께 (NOTES 섹션 추가)

### Step 5 — 랜딩 재설계 (3일)

- 2카드 분기 → 단일 드롭존 + 자동 추천 (PHASE1_PLAN 3-1 단계 1)
- 헤드라인 세리프 디스플레이
- 재방문자 메타 정보 하단 노출

### Step 6 — 모션 톤 정리 (1일)

- 모든 hover scale 제거
- 트랜지션은 opacity + border-color만
- 갤러리 stagger fade-in

---

## 7. 디자인 결정의 갈림길에서 묻는 5가지 질문

작업 중 결정이 애매해질 때마다 다음 질문에 통과해야 한다:

1. **"이게 라이트룸 같은가, 카카오톡 같은가?"** — 라이트룸이면 OK
2. **"갤러리 도록에서 봤다면 어색한가?"** — 어색하지 않으면 OK
3. **"큐레이터가 이렇게 말할까?"** — 안 할 것 같으면 카피 다시
4. **"이모지/느낌표가 있는가?"** — 있으면 빼기
5. **"숫자가 자신 있게 박혀 있는가?"** — 어물쩍 넘기지 않기

---

## 8. PHASE1_PLAN.md와의 관계 정리

- **기능 스펙**(어떤 기능을 만들지)은 `PHASE1_PLAN.md` 그대로 유효
- **시각 언어와 카피**(어떻게 보일지)는 이 문서가 우선
- 충돌하는 카피 예시는 §5-2 표에 정리됨. PHASE1_PLAN의 카피는 점진적으로 이 톤으로 교체
- 사진 상세 모달(⑥)은 §4-4의 NOTES 레이아웃으로 구현

---

> 이 디자인 방향은 **점진적으로** 적용한다. 기능 한 번에 다 못 바꿔도, **이모지 빼기 + 다크 토큰 + 세리프 헤드라인** 세 가지만 v0.4.0에 들어가도 사용자 인상은 크게 바뀐다.

---

# 9. v2 보강 (2026-05-04) — 전문성 · AI 묻은 기술 · 모바일

> **이 §9~§11은 §1~§8 골격 위에 다음 세 가지를 채운다:**
> 1. 가이드와 실제 코드의 격차(특히 Landing.tsx) 해소
> 2. "**전문가에게 맡긴다는 느낌**" 강화 — 큐레이터 모티프를 신뢰감 사양으로 구체화
> 3. "**AI가 묻은(녹은) 기술**" 톤 — AI를 표면에 노출하지 않으면서도 작동을 자연스럽게 보여주는 원칙
> 4. **모바일 가이드** (Stage A에서 누락) — 데스크톱 우선·모바일 축소 정책
>
> **사용자 결정 (2026-05-04):**
> - 디자인 기준 뷰포트: **데스크톱 우선 + 모바일 축소** (1280×800 기준 / 모바일은 적응형)
> - 액센트 컬러: **박물관 골드 교체** (§9-2에서 후보 3안 → 최종 결정)

---

## 9-1. 가이드와 코드 갭 — 즉시 해소 패치 (v0.5.1 우선)

가이드(§1~§8)는 큐레이터 톤·이모지 금지·직각·보더만 호버를 명시하지만, 실제 `src/components/Landing.tsx`는 5가지 항목에서 가이드를 위반한 상태로 머물러 있다. Stage A에서 토큰만 정의하고, Stage B에서 컴포넌트 마이그레이션이 부분적으로만 완료되었다. **`globals.css`의 헤더 코멘트에 "Stage B 후속 작업 필요"로 명시되었으나 머지 후에도 잔존**.

### 9-1-A. 위반 5건 (Landing.tsx 기준)

| # | 가이드 원칙(§참조) | 위반 위치 | 변경 사양 |
|---|------|----------|----------|
| 1 | §3-6 이모지 추방 | `CARDS[].emoji: "📷" / "📁"`, 헤더 `"✦ AI Photo Selector · Beta"` | 이모지 제거. 카드 라벨은 텍스트만 (`tag`만 사용) / 헤더 뱃지는 `BETA` 한 단어 또는 제거 |
| 2 | §3-4 카드 radius `--radius-md: 4px` | 카드 `borderRadius: 20`, 뱃지 `borderRadius: 20` | 카드 4 / 뱃지 2 (`var(--radius-md)`, `var(--radius-sm)`) |
| 3 | §3-4 카드 그림자 사용 금지 | hover `boxShadow: 0 20px 48px rgba(108,99,255,0.22)` | `boxShadow: none`. 호버 신호는 보더만 |
| 4 | §3-5 hover scale·translate 금지 | hover `transform: translateY(-5px)` | `transform` 제거. `border-color` + 미세한 `opacity 0.95`만 |
| 5 | §3-1 단일 액센트 | `accentRgb: "108,99,255"`, `var(--accent2)` 잔존 | §9-2에서 결정될 새 액센트로 교체 (RGB 하드코딩 제거 → 토큰 직접 참조) |

### 9-1-B. 토큰 이중화 통합

현재 `src/styles/tokens.css`(canonical)와 `src/globals.css`(레거시 별칭) 둘 다 활성. 컴포넌트가 어떤 변수를 참조해야 할지 일관성이 없다.

**통합 정책:**
- **신규 코드:** canonical 토큰만 사용 (`--bg-base`, `--accent`, `--text-primary`)
- **기존 코드:** 레거시 별칭(`--bg`, `--accent2`)은 v0.5.1까지 유지하되, 같은 PR 안에서 발견되면 canonical로 교체
- **목표:** v0.6.0 시점에 globals.css의 별칭 블록 전체 제거

### 9-1-C. 검증 명령

```bash
# 이모지 잔존
grep -rnE "[📷📁📦🎯💎📌👋🎉🔁🔒✨🪄✦]" src/components/ src/messages/

# borderRadius 비정상 값 (가이드 허용: 0, 2, 4, 8)
grep -rnE "borderRadius:\s*(1[0-9]|[2-9][0-9])" src/components/

# boxShadow 사용 (모달 외 금지)
grep -rn "boxShadow" src/components/ | grep -v "PhotoModal\|Toast"

# hover translate
grep -rn "transform.*translateY" src/components/

# 보라 RGB 잔존
grep -rnE "(108,\s*99,\s*255|6c63ff|7c6ff7)" src/
```

위 5개 grep이 모두 출력 0이 되어야 v0.5.1 진입 가능.

---

## 9-2. 액센트 컬러 재정의 — 골드 교체

> **결정 배경:** 박물관 골드(#C9A961)는 큐레이터 모티프와는 잘 맞지만, 사용자가 추가로 요청한 "**AI가 묻은(녹은) 기술**" 톤과 충돌한다. 골드는 럭셔리·고가 시그널이 강해서 도구·연구·정확함의 인상을 흐린다. 컬러 자체를 더 침묵하고 신뢰감 있는 톤으로 교체한다.

### 9-2-A. 후보 3안 비교

#### 안 A — 잉크 블루 `#2D4356` ⭐ Recommended

| 속성 | 값 |
|---|---|
| 메인 | `#2D4356` |
| Soft (12%) | `rgba(45, 67, 86, 0.12)` |
| Dim (보조) | `#3E5670` |
| 인상 | 신문 인쇄·연구실·헬스케어·금융 보고서 |
| 적합도 | ◎ "AI가 묻은 기술" + "전문성" 모두 충족. 사진 위에 얹혀도 시끄럽지 않음. 어두운 배경(#0E0D0B)과 콘트라스트 충분 |
| 위험 | 너무 차가워질 가능성 → `#2D4356`보다 살짝 따뜻한 `#324A60`도 후보 |

#### 안 B — 청록 `#3D5A5A`

| 속성 | 값 |
|---|---|
| 메인 | `#3D5A5A` |
| Soft (12%) | `rgba(61, 90, 90, 0.12)` |
| Dim | `#506E6E` |
| 인상 | 청동 과학박물관·수술실·고전 카메라 바디 |
| 적합도 | ○ 전문성 강력. AI 묻은 기술 OK. 다만 골드보다도 더 묵직 → 가벼운 일상 사진(여행)에선 무거울 수 있음 |
| 위험 | 인물 사진 톤과 색온도 충돌 가능성 |

#### 안 C — 따뜻한 회색 `#7A6F5C` (거의 무채색)

| 속성 | 값 |
|---|---|
| 메인 | `#7A6F5C` |
| Soft (12%) | `rgba(122, 111, 92, 0.12)` |
| Dim | `#8E8470` |
| 인상 | 도록 표지·필름·오래된 종이 |
| 적합도 | ◎ 가장 침묵. AI 묻은 기술 톤에 완벽 일치 (액센트가 거의 안 보임) |
| 위험 | 결제 CTA·진행 상태 강조에서 신호가 약해질 수 있음. CTA는 `--text-primary` 흰색 + 배경 elevated로 대신 처리 필요 |

### 9-2-B. 추천 — 안 A (잉크 블루)

세 가지 모두 골드보다 사용자 요구에 가깝지만, **잉크 블루가 균형이 가장 좋다.** 이유:
1. **CTA 신호 강도 충분** — 결제·진행 상태에서 자연스럽게 눈에 든다
2. **사진 위 메타 라벨 톤과 잘 어울림** — 청사진/도구의 느낌
3. **다크 배경에서 선명** — 따뜻한 회색은 묻혀버릴 위험 있음
4. **광고·결제 모듈 도입 시 톤 충돌 적음** — 골드는 "프리미엄" 인상이 너무 강해 광고와 충돌

> **최종 결정은 사용자가 한다.** 이 §9-2 패치를 적용할 때 사용자가 안 A/B/C 중 1개를 골라 `--accent` 토큰 값으로 확정한다.

### 9-2-C. 적용 대상 (안 무관)

```
src/styles/tokens.css
  --accent       → 새 메인
  --accent-soft  → 새 메인의 12% 알파
  --accent-dim   → 새 보조

src/globals.css
  .btn-primary, .progress-fill, .photo-card.selected → 자동 반영
  .badge-high (현재 #22c55e 형광 그린) → 새 액센트 또는 #6B8B5A 같은
                                          올드 그린으로 교체 (가이드 §3-1에서
                                          형광 금지 명시 → 형광 그린은 자체로 갭)

src/components/Landing.tsx
  CARDS[].accentRgb 하드코딩 제거 → var(--accent) 직접 참조
  hover 시 border-color: var(--accent)
```

---

## 9-3. "전문가에게 맡긴다는 느낌" — 5신호 사양

> 큐레이터 모티프(§1~§8)는 톤의 비유였다. 이 §9-3은 그 비유를 **시각·상호작용 사양**으로 구체화한다. 의료 진료 명세·법률 의견서·박물관 도록의 디테일을 차용한다.

### 9-3-A. 다섯 가지 신호

#### ① 수치로 말한다 (자랑하지 않고 사실만)

```
✗  "1,247장 → 80장으로 정확히 골라드렸어요!"
○  "1,247  사진을 받았습니다"
   "  80   셀렉되었습니다"
   "   3분 17초                      소요"
```

- 모든 수치는 **모노 폰트 + 우정렬**
- "정확히", "완벽히", "최고로" 같은 형용사 금지
- 단위는 한글 표기 ("80**장**"), 숫자만 모노

#### ② 이력으로 말한다 (조용히, 푸터 또는 메타 영역)

```
지난 30일 동안 1,847장에서 124장 셀렉.
누적 23회 작업 · 평균 소요 4분 12초.
```

- **자랑 위치 금지:** 히어로 영역·CTA 위 X
- **권장 위치:** 랜딩 푸터, 갤러리 좌측 메타 패널 하단, 결과 화면 끝
- 톤: 사실 진술. "벌써 N번이나 사용해주셨어요!" X

#### ③ 도구의 침묵 (작업 중·완료 시 시스템이 떠들지 않음)

```
✗  "✨ 셀렉 완료! 환상적이에요 🎉"
○  "완료."
```

- 분석 끝나면 단어 하나 + 결과 표
- 토스트 알림은 "결제 완료" / "원본 ZIP · 80장 · 312 MB" 같은 명세형
- 애니메이션은 페이드만 (§3-5 그대로)

#### ④ 명세 톤 (의료·법률 보고서)

`PhotoDetailModal`의 NOTES 섹션이 정확히 이 톤(§4-4 그대로). 추가 적용 영역:

```
[FolderGallery 좌측 메타 패널]
─────────────────────────────────
SELECTED · 80
─────
만삭          12 / 15
베이비본      23 / 25
100일         18 / 30
돌            27 / 40
─────────────────────────────────
REJECTED · 1,167
─────
눈 감음        32
흔들림         18
중복           124
저조도          7
기타          986
```

- 라벨 대문자 트래킹 `--tracking-uppercase: 0.12em`
- 모노 우정렬 + 하이픈 구분선

#### ⑤ 시그니처 디테일

| 위치 | 패턴 | 예시 |
|---|---|---|
| 폴더 표기 | 슬래시 + 공백 | `만삭 / 베이비본 / 100일 / 돌` |
| 진행률 | 모노 `%` 우측 | `47%` (모노, 정렬) |
| 점수 | `N / 100` 분수형 | `62 / 100` |
| 파일명 | 모노 + 확장자 회색 | `IMG_2871**.jpg**` |
| 시간 | 24h 표기 | `14:32 · 4분 17초` |
| 카운터 강조 | 큰 숫자 + 작은 라벨 | `**80** 장` (display + sm) |

### 9-3-B. 레퍼런스 추가

| 분야 | 차용할 디테일 |
|---|---|
| 의료 진료 명세서 | 표 정렬·항목별 단위 표기·소수점 자릿수 |
| 법률 의견서 | 인용 번호·각주 스타일·"이상" 마무리 |
| 박물관 도록 | 작품 캡션(`작가 · 연도 · 매체 · 크기`) |
| 카메라 메타데이터 | EXIF 표기(`f/1.4 · 1/200s · ISO 400`) |
| Linear 이슈 | 단축키 노출·키보드 우대 |

---

## 9-4. "AI가 묻은(녹은) 기술" — 디자인 원칙

> 가이드 §5-4는 "AI 단어 자제" 한 단락만 있다. 사용자가 원하는 "AI가 묻은 기술"은 더 적극적인 사양을 요구한다. 핵심: **AI가 작동하고 있다는 사실은 분명한데, 마케팅으로 떠들지 않는다.**

### 9-4-A. 보조자 노선 (마법사 노선 명시 거부)

| 마법사 노선 (피할) | 보조자 노선 (택할) |
|---|---|
| ✨ 별빛 효과·스파클 애니메이션 | 정적 진행률 바 + 모노 % |
| "AI가 똑똑하게 골라드려요" | "셀렉이 끝났습니다." |
| 마법봉·로봇·뇌 아이콘 | 아이콘 없음 또는 라인 도구 아이콘 |
| 무지개 그라데이션 | 단일 액센트 |
| "Magic" / "AI Magic" / "Smart" | (사용 안 함) |
| 처리 결과를 마법으로 포장 | 처리 단계와 수치를 그대로 노출 |

### 9-4-B. AI 마케팅 금지어 사전

다음 단어/표현은 코드·카피·메시지 어디서도 사용하지 않는다:

```
스마트 / 지능형 / 혁신적 / 마법 / Magic / Smart / Intelligent /
한 번에 / 놀라운 / 똑똑한 / 환상적 / 완벽한 / 최고의 /
딸깍 한 번에 / 뚝딱 / 알아서 / 자동으로 (자제, 가능하면 빼기) /
AI가 골라드려요 / AI가 분석 중 (주어 교체)
```

검증 명령:
```bash
grep -rnE "(스마트|지능형|혁신적|마법|Magic|Smart|Intelligent|한 번에|놀라운|똑똑한|환상적|완벽한|최고의|딸깍 한 번에|뚝딱|알아서|AI가 골라|AI가 분석)" src/messages/ src/components/
```

> 출력 발견 시 `messages/ko.json` 우선 정정. 코드 내 한국어 하드코딩이면 `messages`로 이전 후 정정.

### 9-4-C. "AI" 단어 사용 한도

- **페이지당 최대 1회**, 가능하면 0회
- SEO 메타 태그(`title`, `description`)는 검색 의도 매칭 위해 1회 허용 (현재 `index.html` 그대로 유지 OK)
- UI 본문/카피는 "딸깍픽스는…" 또는 수동태로 주어 교체:

| Before | After |
|---|---|
| AI가 골라드려요 | 셀렉이 끝났습니다 |
| AI가 분석 중입니다 | 분석 중 |
| AI 보정 | 리터치 |
| AI 추천 | 추천 |
| 이 AI는 어떻게 작동하나요? | 어떻게 작동하나요? |

### 9-4-D. 작업 투명성 (블랙박스 X, 명세 O)

AI를 마법화하지 않는 가장 효과적인 방법은 **작업 과정을 명세로 노출**하는 것이다. 이미 PHASE1_PLAN §1-1·DESIGN_DIRECTION §4-2에서 이 방향을 잡았으니, 그 카피 톤을 §9-4 원칙으로 승격:

```
○  단계명 + 실수치 카운터
   "1,247   사진을 받았습니다"
   "  124   그룹으로 묶었습니다"
   "   32   눈 감은 컷"

✗  추상화·이모지·진행률만
   "✨ AI가 분석 중... 47%"
```

### 9-4-E. 권장 / 피할 레퍼런스

| 권장 | 피할 |
|---|---|
| **Linear** — 정확한 톤, 단축키, 모노 디테일 | NVIDIA 마케팅 페이지 — 미래주의·블랙 + 네온 |
| **Apple Intelligence (2024+)** — 드러나지 않는 도구 | Stable Diffusion 데모 — 마법 그라데이션 |
| **Things 3** — 침착한 인터페이스 | 일반 "AI 부스러기" SaaS — Smart·Magic 태그 도배 |
| **Adobe Lightroom** — 프로용 도구 | Duolingo·Toss 일러스트 — 친근감 과잉 |
| **GitHub Copilot UI** | "AI Photo Enhancer" 광고 — Before/After 자랑 |

---

# 10. 모바일 가이드 (Stage A 누락분 보강)

> **정책:** 데스크톱 우선 + 모바일 축소.
>
> **이유:** 핵심 작업(스튜디오 셀렉 = 폴더 여러 개·1,000장+ 프리뷰·앨범 배치)은 본질적으로 큰 화면이 유리하다. 모바일은 보조형 — Flow A(개인 셀렉)와 결과 확인까지만 깔끔하게 동작하면 된다.
>
> **단, 모바일에서 "깨지지 않는" 것은 절대 조건.** 기능을 못 쓰는 화면이 있어도 OK, 깨진 레이아웃은 불가.

---

## 10-1. Breakpoints

```css
:root {
  --bp-mobile:  480px;   /* 모바일 (단일 컬럼) */
  --bp-tablet:  768px;   /* 태블릿 (2~3 컬럼) */
  --bp-desktop: 1024px;  /* 데스크톱 (기본) */
  --bp-wide:    1280px;  /* 디자인 기준 ⭐ */
}
```

**기준 뷰포트는 1280×800.** 와이드(>1440)는 콘텐츠 max-width로 가운데 정렬.

## 10-2. 모바일 타이포 스케일 (`<768px`)

```css
@media (max-width: 768px) {
  :root {
    --display-1: 36px;   /* 56 → 36 */
    --display-2: 48px;   /* 80 → 48 */
    --text-xl:   24px;   /* 36 → 24 */
    --text-lg:   20px;   /* 24 → 20 */
    --text-md:   16px;   /* 18 → 16 */
    --text-base: 16px;   /* 15 → 16 (모바일 가독성) */
    --space-24:  48px;   /* 96 → 48 */
    --space-16:  32px;   /* 64 → 32 */
  }
}
```

또는 `clamp()`로 일괄:
```css
--display-1: clamp(36px, 5vw, 56px);
--display-2: clamp(48px, 7vw, 80px);
```

## 10-3. 터치 타겟 최소 크기

- 버튼·탭·카드 클릭 영역 **최소 44×44 px** (iOS HIG)
- 텍스트 링크는 상하 패딩 8px+ 추가
- `<480` 환경에서 sticky bar 액션 버튼은 **height 56px 이상**

## 10-4. 화면별 모바일 적응

### 랜딩
- 카드 그리드 → 단일 컬럼 stack (gap 12px)
- 헤드라인 줄바꿈 적극 (3~4줄까지)
- 모바일에선 "스튜디오용 셀렉" 카드 진입 시 **상단 안내 배너** 노출:
  ```
  대용량 폴더 작업은 데스크톱을 권장합니다.
  PC에서 같은 주소로 이어 작업할 수 있습니다.
  ```
- 자동 분기(드롭) 기능은 모바일에선 **사진앨범 시트**(input file accept=image/*)로 폴백

### 폴더 탭 (FolderGallery)
- 가로 스크롤 + 좌/우 페이드 그라데이션
- 활성 탭 자동 중앙 정렬 (`scrollIntoView({inline: 'center'})`)
- 탭 라벨 풀 텍스트 → 모바일에선 짧게 (`만삭 12/15` → `만삭 12`)

### 갤러리 그리드
| 뷰포트 | 컬럼 | 비율 |
|---|---|---|
| `>1280` | 5 | 1:1 |
| `1024-1279` | 4 | 1:1 |
| `768-1023` | 3 | 1:1 |
| `480-767` | 3 | 1:1 |
| `<480` | 2 | 4:5 (인물 친화) |
- 갭 1px 유지 (모바일 0.5px도 OK)

### 사진 상세 모달
- **데스크톱:** 가운데 모달 (max-width 960px)
- **모바일:** 풀스크린 슬라이드업 시트
  - 헤더 위치 sticky top, 액션 버튼 sticky bottom
  - 줌은 핀치 + 더블탭만 (스크롤휠 X)
  - swipe right 닫기

### Sticky CTA
```css
.sticky-cta {
  position: sticky;
  bottom: 0;
  padding-bottom: calc(16px + env(safe-area-inset-bottom));
  background: var(--bg-base);
  border-top: 1px solid var(--border-subtle);
}
```
모바일에서는 화면 하단 sticky, 데스크톱에서는 갤러리 우하단 모듈로 노출.

### Drive 다운로드 배너
모바일에서 가로폭 차지가 크므로:
- 진행률 + 폴더명만 표시 (개별 진행 항목은 탭으로 펼치기)
- 닫기 버튼 위치 우상단 고정

## 10-5. iOS Safari 특화

체크리스트:
- [ ] `100vh` → `100dvh` (Dynamic Viewport, iOS 16+ 호환)
- [ ] Safe Area: `env(safe-area-inset-top/bottom/left/right)` 적용 (Notch 대응)
- [ ] `-webkit-tap-highlight-color: transparent;` (호버 회색 제거)
- [ ] `touch-action: manipulation;` 버튼·카드에 (300ms 딜레이 제거)
- [ ] `<input type="file">` 모바일 동작: `accept="image/*"`만 사용. `webkitdirectory`는 모바일 미지원이므로 폴더 업로드 UI는 모바일 자동 숨김
- [ ] `position: fixed` + 스크롤 컨테이너는 iOS 키보드 등장 시 깨지므로 모달은 `position: absolute` + body lock 패턴

## 10-6. 모바일에서 "안 되는 것" 명시

다음은 모바일 차단 + 안내 노출:

| 기능 | 모바일 동작 |
|---|---|
| 폴더 통째 드래그앤드롭 | 차단 + "PC에서 작업하시는 게 편합니다" 안내 |
| 다중 폴더 동시 업로드 | 차단 + 단일 폴더만 |
| Google Drive 폴더 통째 다운로드 | 가능하나 4G/LTE에선 진행 전 데이터 사용량 경고 |
| 앨범 배치 (AlbumContainer) | 차단 + "데스크톱 전용" 안내 |
| 1,000장+ 분석 | 가능하나 메모리 경고 + 진행률 우선 |

---

# 11. 우선 적용 패치 매트릭스

## 11-1. v0.5.1 (Stage D1 잔여와 함께 들어감)

| 카테고리 | 항목 | 비용 |
|---|---|---|
| 갭 해소 | Landing.tsx 이모지 5건 제거 | 작음 |
| 갭 해소 | borderRadius 20 → 4·2 | 작음 |
| 갭 해소 | hover boxShadow·translateY 제거 → 보더+opacity | 작음 |
| 갭 해소 | accentRgb 하드코딩 제거 → var 참조 | 작음 |
| 컬러 | 골드 → 사용자 선택 액센트로 토큰 교체 | 작음 (토큰만) |
| 컬러 | `.badge-high` 형광 그린 → 차분한 그린 | 작음 |
| 전문성 | FolderGallery 좌측 메타 패널 신설(§9-3-A ④) | 중 |
| 전문성 | 결과 화면 마지막 한 줄 큐레이터 톤 | 작음 |
| AI 톤 | 금지어 사전 grep 통과 + ko.json 정정 | 작음 |
| 모바일 | Breakpoint + clamp 타이포 | 작음 |
| 모바일 | iOS Safe Area + dvh | 작음 |
| 모바일 | 모바일 차단 안내 (앨범 배치) | 작음 |

## 11-2. v0.6.0 (Stage D2 같이)

| 카테고리 | 항목 | 비용 |
|---|---|---|
| 갭 해소 | globals.css 별칭 블록 제거 + 모든 컴포넌트 canonical 토큰 마이그레이션 | 중 |
| 전문성 | 누적 통계 푸터(§9-3-A ②) | 중 |
| 전문성 | 시그니처 디테일(§9-3-A ⑤) 일괄 적용 | 중 |
| 모바일 | 폴더 탭 가로 스크롤 + scrollIntoView | 중 |
| 모바일 | 사진 상세 풀스크린 슬라이드업 시트 | 중 |
| 모바일 | Drive 배너 모바일 컴팩트 모드 | 중 |

## 11-3. 미정 (필요 시 별도 PR)

- 라이트 모드 (Phase 1에서 만들지 않음, §3-1 그대로 유지)
- 한국어 명조 폰트(Noto Serif KR) 가중치 조정 — 현재 300/400/600 로드, 필요 시 700 추가
- 단축키 시스템 (§9-3-B Linear 차용 — Phase 2 영역)

---

## 9-5. v2.1 보강 (2026-05-08) — 헤드라인 폰트 결정 변경

> **결정 (2026-05-08):** §3-2의 "헤드라인은 세리프(Spectral·Noto Serif KR)" 정책 → **산세리프 Pretendard 굵게(fontWeight 800)로 변경**.
>
> **변경 이유:**
> 1. 한글 명조(Noto Serif KR)와 영문 세리프(Spectral)가 **시각적 무게가 달라** 한글·영문 혼용 헤드라인에서 톤 통일 깨짐
> 2. 후속 페이지(`StudioTypeSelect` 등)가 이미 Pretendard 800·900을 사용 중이라 **랜딩 헤드라인만 세리프**라서 사용자 이동 시 폰트 점프
> 3. 모바일에서 명조 가독성 떨어짐 (특히 작은 사이즈)
> 4. 큐레이터 톤은 폰트가 아니라 **카피·여백·직각·단색 톤**으로 충분히 표현됨

### 적용 사양

```css
/* 디스플레이 헤드라인 톤 (Display 컴포넌트와 직접 참조 모두) */
font-family: var(--font-sans);    /* Pretendard */
font-weight: 800;                  /* 굵게 — 한글에서 충분한 무게 */
font-style: normal;                /* 한글 italic 금지 */
letter-spacing: var(--tracking-display);  /* -0.02em — 산세리프 굵게에서도 적절 */
```

### `--font-display` 토큰 정책

- 토큰 자체는 **삭제하지 않고 보존** (미래에 다시 세리프로 가거나 부분 사용 가능성 대비)
- **모든 컴포넌트는 `--font-display` 대신 `--font-sans` 직접 참조**
- v0.5.2.1 시점 grep 결과: `var(--font-display)` 직접 참조 0건 보장

### Display 컴포넌트 동작 (v0.5.2.1 이후)

- `italic` prop 시그니처는 호환성 유지 (호출부 변경 0)
- 동작은 `fontWeight 800 → 600`으로 살짝 가벼워지는 것만. `fontStyle: italic` 적용 안 함 (한글 어색)

### Spectral·Noto Serif KR 폰트 로드

`index.html` Google Fonts 로드 자체는 **유지** (작은 텍스트 영역에서 의도적으로 부분 사용 가능 — 인용·캡션 등). 그러나 헤드라인 디폴트는 산세리프.

---

# 12. 의사결정 갈림길 — v2 추가 질문

§7의 5가지 질문에 다음을 추가:

6. **"이게 도구인가, 마법사인가?"** — 도구면 OK, 마법사면 빼기
7. **"AI라는 단어가 페이지에 두 번 이상 보이는가?"** — 그렇다면 한 번 빼기
8. **"모바일에서 가로 스크롤이 발생하는가?"** — 발생하면 컨테이너 max-width·overflow 검사

이 8가지 질문이 모두 통과하면 v2 톤에 부합한다.

---

> **v2 적용 원칙:** §9-1 갭 해소는 **이번 PR(v0.5.1)에 무조건 포함**. §9-2 컬러 결정은 사용자 확정 후 같은 PR에. §9-3·§9-4·§10은 점진 적용 — 새 화면을 만들 때마다 이 사양을 반드시 참조한다. §11 매트릭스가 그 분배표다.
